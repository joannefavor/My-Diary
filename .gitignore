# 비밀 열쇠 — 절대 올라가면 안 된다
.env*
.vercel

# 도구가 만드는 로컬 파일 — 옮길 필요 없다
.DS_Store
.omc
screenshots/

# 창고(Upstash) 연결 때 딸려 온 사용 설명서 — 웹사이트와 무관하다
.agents/
.claude/
skills-lock.json

# 설치한 꾸러미. 목록표(package.json)만 올리면 상대 쪽에서 다시 받으므로 옮기지 않는다
node_modules/
.next/
