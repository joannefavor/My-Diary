# 주간 기록부 — 개발 로그 (기기 간 동기화 · 홈 화면 앱)

AI 코딩 도구와 함께 진행한 개발 작업 기록입니다.
기존 `DEVLOG.md`(7/25~7/31, 작업 1~26번)에 이어지는 별도 기록입니다.

**사용 도구**: Claude Code (Opus 5, 1M context)

이날의 줄기는 셋입니다.

1. **기기 간 기록 동기화 완성** — 같은 비밀 코드를 넣은 기기끼리 기록을 맞추되, 서버는 내용을 못 보게
2. **스터디 배포 미션** — 스터디장님이 주신 마스터 프롬프트를 **새 프로젝트를 만드는 대신 기존 대시보드에 대조해,
   적용 가능한 것만 골라 적용** (섹션 9-1의 대조표). 이 과정에서 반응형이 폰에서 동작하지 않던 문제를 발견
3. **홈 화면 앱(PWA)** — 아이콘을 눌러 앱처럼 열리고, 인터넷 없이도 동작

---

## 2026-08-01 (Day 5)

### 1. 이어서 할 작업 고르기

```
하이
이기서 다시 작업 시작할게
```

**Claude Code 작업:**
- 폴더 상태를 점검 — `health_care`에서 `dairy_report`로 폴더를 옮긴 뒤였는데, git 이력·GitHub 리모트·Vercel 링크가 모두 정상 유지된 것을 확인
- `HANDOFF.md`에 남겨둔 "남은 일" 4가지를 선택지로 제시

**기획 Q&A:**

```
질문: 어떤 작업부터 이어서 할까요?
선택지: 기기 간 기록 동기화 / 휴대폰 사용성 개선 / AI_CASE_STUDY.md 검토 / 실제 일정 채우기
```
- 선택: **기기 간 기록 동기화**

---

### 2. 동기화 방식 결정 — 프라이버시와 맞바꿀 것을 먼저 정리

**Claude Code 작업:**
- 이 프로젝트의 두 가지 원칙과 충돌한다는 점을 먼저 제시
  - 의존성 없음 / 서버 없음 (`index.html` 한 파일이 전부)
  - 기록이 브라우저 밖으로 안 나감 (프로덕션 주소가 공개인데도 안전했던 이유)
- 세 가지 방식의 장단점을 정리해 선택지로 제시

**기획 Q&A:**

```
질문: 기기 간 동기화를 어떤 방식으로 갈까요?
선택지:
  - 클라우드 자동 동기화 (추천) — 비밀 코드로 암호화, 아이폰 포함 모든 기기 동작
  - 클라우드 드라이브 파일 연결 — 서버 불필요하나 아이폰 사파리에서 불가
  - 먼저 수동 전송을 쉽게 — QR·링크로 개선
```
- 선택: **클라우드 자동 동기화**

---

### 3. 저장소 선정과 프로비저닝

**Claude Code 작업:**
- Vercel `marketplace` 스킬을 먼저 로드 → 저장소는 전용 스킬(`vercel-storage`) 담당이라는 안내를 받고 그쪽으로 이동
- `vercel integration discover --category storage`로 후보 조회
- **Upstash for Redis** 선정 — 키-값 하나에 JSON을 넣는 용도에 맞고, **REST API가 있어 `fetch`만으로 호출 가능**하여 "의존성 없음" 원칙을 그대로 유지할 수 있음
- 프로비저닝이 브라우저 약관 동의를 요구 → 사용자 액션 대기

```
주소가 사라졌어
```

- 약관 동의 링크는 만료된 것이 아니라 고정 링크임을 확인하고, `open` 명령으로 기본 브라우저에 직접 띄워 복사 부담을 제거

```
됐어
```

- `vercel integration add upstash/upstash-kv --no-claim --name my-diary-sync` 실행 → 리소스 생성 및 `my-diary` 프로젝트 연결 완료
- `.env.local` 자동 생성 및 `.gitignore` 자동 등록 확인 (`KV_REST_API_URL`, `KV_REST_API_TOKEN`)

---

### 4. 서버 함수 작성 — 내용을 모르는 중계소

**Claude Code 작업:**
- `api/sync.js` 생성 — 의존성 0, Upstash REST를 `fetch`로 직접 호출
- 설계: 서버는 **암호문 덩어리를 그대로 받아 그대로 돌려주기만** 함. 비밀 코드도 복호화 열쇠도 서버로 오지 않음
- `GET`은 조회, `PUT`은 저장. id는 64자 hex만 허용
- **동시 저장 방지**: Lua 스크립트로 "내가 알던 판 번호가 아직 최신일 때만" 저장(원자적 CAS). 어긋나면 409 반환 → 클라이언트가 다시 받아 합친 뒤 재시도
- IP당 분당 120회 속도 제한, 900KB 크기 상한, 2년 TTL(쓸 때마다 갱신)

---

### 5. 암호화 계층 (Web Crypto)

**Claude Code 작업:**
- 비밀 코드 → PBKDF2(SHA-256, 31만 회, OWASP 권고치) → 512비트 도출
  - 앞 32바이트 → **AES-GCM 열쇠** (이 기기 밖으로 나가지 않음)
  - 뒤 32바이트 → **저장소 id** (이것만 서버로 전송)
  - PBKDF2 블록은 서로 독립이라 id를 알아도 열쇠를 역산할 수 없음
- 안전한 코드 자동 생성기 — 헷갈리는 글자(`0 o 1 l i`) 제외한 31자 알파벳으로 12자리(`k7m2-9xqp-4vwz` 꼴, 약 59비트)
- 코드 입력은 대소문자·공백을 흡수(`normCode`)하여 기기 간 불일치 방지
- 큰 기록에서 스택이 넘치지 않도록 base64 변환을 8KB씩 나눠 처리

---

### 6. 날짜 단위 병합 — 두 기기에서 각각 적어도 둘 다 남게

**Claude Code 작업:**
- 하루 기록마다 수정 시각을 넣는 대신, **마지막으로 맞춘 시점(base)을 기준 삼는 3-way 병합** 채택 — 데이터 형식을 바꾸지 않아도 되고, git과 같은 방식
- 판정 규칙
  - 한쪽만 바뀜 → 바뀐 쪽 채택
  - 양쪽 다 바뀜 → 이 기기 것을 남기고 **몇 건인지 사용자에게 알림** (합칠 방법이 없으므로 숨기지 않음)
  - 삭제도 동일하게 처리
- 키 순서에 흔들리지 않는 안정적 직렬화 함수(`stable`) 작성. 없는 값과 `null`을 구분
- 약 목록은 id 기준 3-way + 이 기기의 순서 유지

---

### 7. 동기화 UI

**Claude Code 작업:**
- 푸터에 "기기 간 동기화" 버튼 → 패널 토글
- 코드 입력칸, 새로 만들기 / 연결 / 연결 끊기 / 지금 맞추기, 상태 표시(정상·경고·오류 3색)
- 기존 CSS 변수와 컴포넌트 스타일을 그대로 재사용해 디자인 언어 일치
- **`HANDOFF.md` 5-(1) 함정 회피**: 받아서 폼을 다시 그리면 적던 내용이 날아가므로, **자동 내려받기를 하지 않음**. 연결할 때·페이지를 열 때·직접 누를 때만 내려받고, 선택한 날이 실제로 바뀐 경우에만 폼까지 다시 그림
- 올릴 예약은 `persist()`가 아니라 **`save()` 안에** 걸어, 수면·혈압처럼 `save()`만 부르는 경로까지 한 번에 덮음

---

### 8. 검증

**Claude Code 작업:**
- 작업 중 발견해 고친 것
  - `stable()`의 sentinel에 **NUL 바이트가 실제로 들어가** 파일이 binary로 취급되던 문제 → 이스케이프 표기로 교체
  - 수면·혈압 입력이 동기화를 건너뛰던 문제 → 예약 지점을 `save()`로 이동
- 검증 내역

| 검사 | 방법 | 결과 |
|---|---|---|
| 병합·코드 처리 25가지 | 헤드리스 크롬, 실제 코드 노출 후 실행 | 전부 통과 |
| 암호화 14가지 | 배포될 코드를 index.html에서 추출해 Node에서 실행 | 전부 통과 |
| 창고 주고받기 6가지 | 로컬 서버 + 실제 Upstash | 전부 통과 |
| 실제 브라우저 왕복 | 헤드리스 크롬 → 창고 → 복호화 대조 | 1초 만에 도착, 내용 일치 |

- 암호화 검사에 포함된 것: 다른 코드로는 안 열림, 한 글자만 변조해도 안 열림, 같은 내용도 매번 다르게 암호화(iv), 1년치 기록 241KB로 한도 안
- 커밋: `e85d5aa` 기기 간 기록 동기화 기능 추가

---

### 9. 배포 미션 — "비개발자 코치" 모드로 전환

```
너는 지금부터 "비개발자 스터디 참가자의 배포 미션 코치"다.
나는 컴퓨터를 쓸 줄은 알지만 코드는 전혀 모르는 완전 초보다.
코드 작업과 터미널 명령은 전부 네가 직접 하고, 나에게는 확인과 클릭만 요청해라.
(중략 — 말투 규칙, 한 번에 한 단계씩, 8단계 순서, 에러 대응 규칙 등)
```

**Claude Code 작업:**
- 프롬프트에 반영되지 않은 것 3가지를 먼저 지적
  1. 이 폴더에는 이미 다른 프로젝트(주간 기록부)가 있어 새 프로젝트를 만들면 충돌
  2. `gh`가 이미 로그인돼 있어 GitHub 웹 절차와 로그인 창이 불필요
  3. 저장 안 된 동기화 작업이 남아 있음

**기획 Q&A:**

```
질문: 새 대시보드를 어디에 만들까요?
선택지: 옆에 새 폴더를 만들기 (추천) / 지금 이 폴더에 그대로 만들기
```
- 답변: **"이 대시보드를 사용할건데 규칙만 적용해줘"** (직접 입력)

```
질문: 어느 쪽을 말씀하신 건가요?
선택지: 프롬프트에 쓴 가짜 데이터 대시보드를 새로 만들자 / 지금까지 만든 주간 기록부를 미션에 쓰자
```
- 선택: **지금까지 만든 주간 기록부를 미션에 쓰자**

- 8단계를 이 프로젝트에 맞게 재구성 (이미 GitHub·Vercel에 올라가 있어 미션 절반이 완료된 상태였음)

---

### 9-1. 마스터 프롬프트를 기존 대시보드에 대조 — 새로 만들지 않고 적용 가능한 것만 골라 적용

스터디장님이 주신 마스터 프롬프트는 **"Next.js로 가짜 데이터 대시보드를 새로 만들라"** 는 내용이었습니다.
그런데 이미 5일에 걸쳐 만든 대시보드(주간 기록부)가 있었기 때문에, 새로 만드는 대신
**프롬프트의 요구사항을 기존 앱에 하나씩 대조해서 적용 가능한 것만 골라 적용**하는 쪽을 택했습니다.

**Claude Code 작업:** 요구사항을 항목별로 코드와 직접 대조

| 마스터 프롬프트 요구사항 | 대조 결과 | 처리 |
|---|---|---|
| 상단에 제목 + **보는 사람 브라우저 기준** 오늘 날짜 | 이미 충족 — `TODAY`를 클라이언트에서 계산 | 그대로 |
| 요약 카드 4개 + 항목명 + 증감 표시 | 이미 초과 충족 — **요약 타일 11개**(주간 평균·합계 기준) | 그대로 |
| 차트 1개 | 이미 초과 충족 — **차트 7종** (수면·혈압·공복혈당·체중·운동·물·기분) | 그대로 |
| 차트 라이브러리를 React 버전에 맞게 선택 | 해당 없음 — **라이브러리 없이 인라인 SVG로 직접 그림** | 그대로 |
| 표 1개 + 상태를 **색상 배지**로 구분 | 이미 충족 — '표로 보기' + 배지. 다만 **색만 쓰지 않고 색·기호(●▲■)·글자를 함께** 씀 | 그대로 (접근성상 더 나음) |
| 카드에 은은한 그림자 | 이미 충족 — `--shadow` 변수 | 그대로 |
| 텍스트는 한글 | 이미 충족 | 그대로 |
| **모바일에서도 안 깨지는 반응형** | **문제 발견** — 좁은 화면용 CSS는 있었으나 `viewport` 태그가 없어 **폰에서 전혀 적용되지 않고 있었음** | **고침** (섹션 11) |
| `.gitignore`에 `node_modules`, `.next`, `.env*` | `.env*`만 있었음 | 나머지 추가 |
| `npm run build` 로 배포 검사 | 이 프로젝트엔 빌드 단계가 없음 | **`vercel build`로 대체** — Vercel이 배포 때 실제로 돌리는 검사라 목적이 같고 더 정확 |
| GitHub 공개 저장소 | 비공개 상태였음 | 공개로 전환 |
| Vercel 배포 → 폰에서 열리는 URL | 이미 배포·연결돼 있었음 | 반영 확인만 |
| 한 번에 한 단계 / 초보자 말투 / 에러는 AI가 처리 | — | **진행 방식으로 그대로 적용** |

**의도적으로 적용하지 않은 것 2가지 (이유와 함께):**

- **Next.js + TypeScript + Tailwind로 갈아엎기**
  → 이 프로젝트는 `index.html` 한 파일에 의존성 0으로 만들어져 있고, 그게 설계상의 핵심 제약입니다.
  프레임워크로 옮기면 5일치 작업을 버리고 처음부터 다시 만들어야 합니다. **미션의 목적(배포된 URL 얻기)에는
  기여하지 않으면서 잃는 것만 큰** 변경이라 제외했습니다.

- **다크 모드 걷어내고 밝은 테마로 고정**
  → 프롬프트는 "기본 템플릿에 다크 모드가 들어 있으면 걷어내라"는 취지였는데, 이 앱의 다크 모드는
  템플릿 잔재가 아니라 **의도적으로 만들어 넣은 기능**(색 변수를 세 스코프에 각각 정의)입니다.
  걷어내면 기능이 후퇴하므로 유지했습니다.

**이 대조에서 얻은 것:** 요구사항을 하나씩 짚어보다가 **"반응형이 된다고 믿고 있었는데 실제로는 폰에서
전혀 동작하지 않던 문제"** 를 발견했습니다. 마스터 프롬프트를 그대로 따랐다면 새 프로젝트를 만드느라
이 버그는 기존 앱에 그대로 남아 있었을 것입니다.

```
질문: 미완성인 동기화 기능을 어떻게 할까요?
선택지: 지금 끝내고 가자 (추천) / 동기화는 잠시 감추고 미션부터 끝내기 / 그냥 이 상태 그대로 배포
```
- 선택: **지금 끝내고 가자**

---

### 10. 미션 8단계 진행

```
잘 보인다
응 다음 단계로 가자
```

**Claude Code 작업:**

| 단계 | 내용 | 결과 |
|---|---|---|
| 1 | 상태 점검 + 창고 연결 완료 | 배포 주소 정상, 저장소 비공개 확인 |
| 2 | 로컬 실행 확인 (`vercel dev`) | `http://localhost:3000` — 화면·API 모두 200 |
| 3 | `.gitignore` 정리 | 비밀 열쇠 차단 확인, 올라갈 파일 4개로 축소 |
| 4 | 커밋 | 38번째 저장, 590줄 |
| 5 | 배포 검사 (`vercel build`) | 통과, 결과물에 열쇠 미포함 확인 |
| 6 | GitHub 공개 전환 + 푸시 | 파일 8개, 금지 항목 0개 |
| 7 | 배포 반영 확인 | 프로덕션에서 창고 왕복 성공 |
| 8 | 폰에서 열기 | 정상 |

- 3단계에서 창고 연동 시 딸려온 `.agents/`, `.claude/`, `skills-lock.json` 40여 개를 차단 목록에 추가. 미션 요구사항인 `node_modules`, `.next`도 안전장치로 미리 추가
- 6단계에서 공개 전환 전, 추적 중인 파일에 실제 건강 수치나 개인정보가 있는지 검사 → 발견된 숫자는 전부 설명용 예시와 대한고혈압학회 기준치임을 확인

```
폰에서 잘 열린다
```

- 미션 제출 양식 작성 (이름: Joan)

**중간에 있었던 자체 실수 (사용자 잘못 아님):**
- `grep -qc $'\0'` — bash가 NUL을 빈 문자열로 만들어 항상 "발견"으로 오판 → Python으로 재검사
- `gh api ...?recursive=1` — zsh가 `?`를 글로브로 해석해 빈 결과 → 따옴표 추가 후 재실행
- `.vercel` 검사 — `.vercelignore` 파일명에 걸린 오탐 → 정확한 경로 패턴으로 재검사

---

### 11. 홈 화면 앱(PWA) — 기본 태그부터

```
폰 홈 화면에 앱처럼 추가하는 것도 해줘
```

**Claude Code 작업:**
- 착수 전 점검에서 **`<!doctype>`, `charset`, `viewport`가 통째로 없다는 것을 발견**
  - `viewport`가 없으면 휴대폰이 데스크톱 폭으로 그린 뒤 통째로 축소 → 좁은 화면용으로 만들어 둔 배치가 **전혀 쓰이지 않음**
  - `HANDOFF.md`에 "좁은 화면 대응"이 동작한다고 적혀 있었으나 실제로는 폰에서 적용되지 않던 상태
- 문서 구조를 갖추고(`<!doctype html>` / `<html lang="ko">` / `<head>` / `<body>`) 세 태그 추가
- **고치기 전후 렌더링을 픽셀 단위로 비교 → 완전히 동일함을 확인** (표준 모드 전환으로 레이아웃이 깨지지 않았음)
- 주소 표시줄 색도 밝은/어두운 테마에 따라 바뀌도록 `theme-color` 2종 추가

---

### 12. 앱 아이콘

**Claude Code 작업:**
- `icon.svg` 작성 — 진한 남색(`#1d3a5f`) 배경에 크림색 상승 곡선 + 주 7일 눈금
- 1차 시안에서 끝점의 원이 선을 끊어 보이는 문제 발견 → 원을 선과 이어지게 다듬음
- 헤드리스 크롬으로 렌더링해 PNG 3종 생성 (192·512·apple-touch 180)
- 안드로이드는 모서리를 둥글게, iOS는 자체 방식으로 잘라내므로 **배경을 꽉 채우고 그림은 가운데 80% 안에** 배치
- 홈 화면 실제 크기(48px)로 줄여 판독성 확인

---

### 13. 앱 설정 파일과 오프라인 지원

**Claude Code 작업:**
- `manifest.json` — 앱 이름 "주간 기록부", `display: standalone`, 아이콘 3종(`any` 2 + `maskable` 1)
- **화면 방향은 일부러 잠그지 않음** — 일정표는 가로로 보는 편이 넓음
- iOS·안드로이드가 서로 다른 표시를 읽으므로 양쪽 메타 태그를 모두 추가
- `sw.js` 작성. 흔한 함정("사본을 잘 챙겨서 고친 내용이 영영 안 보임")을 피하는 전략
  - **화면(HTML)** — 인터넷 우선, 실패 시에만 사본
  - **아이콘·안내문** — 사본 우선
  - **`/api/`** — 사본 사용 금지 (기록은 늘 진짜 창고에서)
  - 판 번호를 붙여 활성화 시 지난 사본 삭제

---

### 14. PWA 검증과 배포

**Claude Code 작업:**
- DevTools 프로토콜로 서비스 워커 등록 확인 → `http://localhost:3000/sw.js` 실행 중
- 사본 보관함 내용 조회 → 6개(화면, 아이콘 4종, 안내문) 전부 담김
- **서버를 완전히 종료한 뒤 새로고침** → 제목·요약 타일 11개·동기화 패널 모두 정상 표시 (오프라인 동작 확인)
- `vercel build` 통과, 결과물에 파일 8개 모두 포함 확인
- 커밋 `7b58885` 후 푸시 → 배포 반영
- 배포 직후 `manifest.json`만 잠시 404 → 롤아웃 진행 중이었던 것으로, 캐시 우회 재요청 시 정상 확인

---

## 커밋 히스토리

| 날짜 | 커밋 | 설명 |
|------|------|------|
| 08/01 | `f72fc4e` | 인수인계 문서(HANDOFF.md) 작성 |
| 08/01 | `e85d5aa` | 기기 간 기록 동기화 기능 추가 |
| 08/01 | `7b58885` | 홈 화면에 앱처럼 추가할 수 있게 함 |

누적 39개 커밋.

---

## 기술 스택

- **Frontend**: 단일 `index.html` (HTML + CSS + 바닐라 JS + 인라인 SVG 차트). 빌드 도구·프레임워크·패키지 매니저 없음
- **암호화**: Web Crypto API — PBKDF2(SHA-256, 310,000회), AES-GCM
- **Backend**: Vercel Functions (`api/sync.js`, 의존성 0, Node.js)
- **저장소**: Upstash for Redis (Vercel Marketplace, REST API 직접 호출)
- **오프라인**: Service Worker (`sw.js`), Web App Manifest
- **Deployment**: Vercel (GitHub `main` 푸시 시 자동 배포)
- **검증 도구**: 헤드리스 크롬(스크린샷·DOM 덤프·DevTools 프로토콜), Node `--check`

---

## 주요 기능

1. **기기 간 기록 동기화**
   - 같은 비밀 코드를 넣은 기기끼리 기록을 맞춤
   - 브라우저 안에서 암호화한 뒤 업로드 — 서버도 저장소 회사도 내용을 열 수 없음
   - 코드에서 열쇠와 저장소 주소를 각각 도출하되, 주소로 열쇠를 역산할 수 없는 구조

2. **3-way 병합**
   - 마지막으로 맞춘 시점을 기준으로 양쪽 변경을 합침
   - 두 기기에서 서로 다른 날을 적으면 둘 다 보존
   - 같은 날을 양쪽에서 고친 경우 이 기기 것을 남기고 건수를 알림

3. **동시 저장 보호**
   - 서버에서 원자적 비교-교환으로 처리해, 두 기기가 동시에 올려도 한쪽이 조용히 덮어써지지 않음

4. **홈 화면 앱**
   - 아이콘을 눌러 주소창 없이 앱처럼 실행
   - 인터넷이 없어도 화면이 열리고 기록 입력 가능
   - 화면은 항상 최신을 우선 받아와 "고쳐도 안 바뀌는" 문제 없음

5. **휴대폰 표시 정상화**
   - 빠져 있던 `viewport` 표시를 채워, 좁은 화면용 배치가 실제로 적용되게 함
