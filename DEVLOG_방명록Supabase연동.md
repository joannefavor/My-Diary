# 주간 기록부 — 개발 로그 (방명록 · Supabase 연동)

AI 코딩 도구와 함께 진행한 개발 작업 기록입니다.
`DEVLOG.md`(7/25~7/31) → `DEVLOG_기기간동기화와홈화면앱.md`(8/1)에 이어지는 세 번째 기록입니다.

**사용 도구**: Claude Code (Opus 5, 1M context)
**기간**: 2026-08-02 ~ 08-03 (Day 6)

이날의 줄기는 둘입니다.

1. **스터디 마스터 프롬프트가 가정한 상황과 실제가 달랐다** — "이 폴더는 Next.js my-dashboard"라고 적혀 있었지만
   실제로는 지난주에 정한 대로 단일 HTML 프로젝트였다. 착수 전에 대조하고 방향을 정했다.
2. **방명록을 붙이며 Supabase·환경변수·RLS를 실제로 다뤘다** — 배울 것은 하나도 빠뜨리지 않되,
   이 프로젝트 구조에 맞는 방식으로 옮겼다.

---

## 1. 착수 전 대조 — 프롬프트가 가정한 폴더가 아니었다

```
너는 지금부터 "비개발자 스터디 참가자의 Supabase 연동 코치"다.
(중략)
[지금 상황]
이 폴더는 지난주에 만든 my-dashboard 프로젝트다.
Next.js(App Router) + TypeScript + Tailwind 로 되어 있고, 이미 GitHub 저장소와 Vercel 배포가 연결돼 있다.
오늘은 여기에 Supabase(인터넷 데이터베이스)를 붙여서, 방문자가 글을 남기면 저장되는 "방명록" 기능을 추가한다.
```

**Claude Code 작업:**
- 1단계(패키지 설치)를 실행하기 **전에** 폴더 상태를 먼저 확인
- `package.json` / `next.config.*` / `tsconfig.json` / `app` / `src` — **전부 없음**. git 리모트도 `my-dashboard`가 아니라 `My-Diary`
- 지난주에 "새로 만들지 말고 기존 주간 기록부를 쓰자"고 정했기 때문에 생긴 차이

| | 프롬프트가 가정 | 실제 |
|---|---|---|
| 프로젝트 | my-dashboard | 주간 기록부 |
| 구조 | Next.js + TS + Tailwind | 단일 `index.html` + Vercel Functions |
| 저장소 | my-dashboard | My-Diary |

- 이 상태로 `npm install` 을 실행하면 **프로젝트 구조와 겉돈다**는 점을 먼저 알리고 선택을 받음

**기획 Q&A:**

```
질문: 방명록을 어느 쪽으로 만들까요?
선택지: 지금 주간 기록부에 붙이기 (지난주와 같은 선택) / 옆에 my-dashboard를 새로 만들기
```
- 선택: **지금 주간 기록부에 붙이기**
- 7단계 순서는 그대로 두고 1단계만 조정. 배울 내용(Supabase 연동 / 환경변수 관리 / RLS)은 그대로 유지

---

## 2. 방명록 붙이기 — 패키지 · 환경변수 · 접속 코드 · 화면

### 패키지 설치

**Claude Code 작업:**
- 의존성이 하나도 없던 프로젝트라 `package.json` 부터 생성 → `npm install` 로 `@supabase/supabase-js` 설치 (9개 패키지, 취약점 0건)
- 설치 전 사전 안내: 1~2분 소요 / `warn`·`deprecated`·`vulnerabilities` 는 실패가 아님 / `npm audit fix` 는 실행하지 말 것
- 설치 직후 확인 — `node_modules`·`.env.local` git 차단 여부, 패키지 로드 여부, 그리고 **`vercel build` 로 기존 사이트가 깨지지 않았는지**

  `package.json` 이 생기면 Vercel 의 프로젝트 판정이 달라질 수 있어 붙이자마자 확인했다. 화면·아이콘·동기화 기능 모두 유지됨.

### 환경변수

**Claude Code 작업:**
- 값을 묻기 전에 "어디에 저장되고 왜 안전한지" 먼저 설명
- **Project URL 을 먼저 묻고 저장한 뒤, anon key 를 따로 물음** (한 번에 하나씩)
- 기존 `.env.local`(지난주 Upstash 값들)을 **덮어쓰지 않고 뒤에 덧붙임**
- **받은 키를 저장 전에 검증** — JWT 페이로드를 해독해 `role` 이 `anon` 인지 확인(마스터 키가 아님), 키 안의 프로젝트 참조값과 URL 이 같은 프로젝트인지 대조

  실수로 마스터 키를 붙여넣는 사고를 막기 위해 기계적으로 확인하는 절차를 넣었다.

**변수 이름에서 프롬프트와 다르게 간 부분** — `NEXT_PUBLIC_` 을 붙이지 않고 `SUPABASE_URL` / `SUPABASE_ANON_KEY` 로 저장

- `NEXT_PUBLIC_` 은 Next.js 에서 **"이 값은 브라우저에 그대로 노출된다"** 는 표시다
- 이 프로젝트는 브라우저가 아니라 **서버가 Supabase 에 접속**하므로 값이 브라우저로 나가지 않는다
- 붙이면 사실과 다른 표시가 되고, 안 붙이는 쪽이 실제 동작과 맞으며 한 겹 더 보호된다
- (참고: anon 키는 원래 노출돼도 되도록 설계된 키다. 실제 문단속은 RLS 가 한다)

### 접속 코드 — 권한이 제한된 대리인

**Claude Code 작업:**
- `api/guestbook.js` — `GET` 은 최신순 100건 조회, `POST` 는 이름·메시지 저장
- **anon 키만 사용. `service_role` 은 쓰지 않음** → 사용자가 걸어 둔 RLS 가 그대로 적용되고, 이 코드가 실수로 수정·삭제를 시도해도 데이터베이스가 거부한다
- **환경변수가 비면 조용히 죽지 않게** 처리 — 어떤 이름이 빠졌는지 알리고 "개발 서버를 껐다 켜야 반영된다"까지 안내. `supabaseUrl is required` 같은 영어 오류로 끝나면 초보가 원인을 찾을 수 없다
- 빈 메시지·길이 초과를 서버에서도 차단 (화면을 거치지 않는 요청이 있으므로)
- 검증: 실제 Supabase 에 연결해 `guestbook` 테이블 조회 성공 (당시 0건)

### 화면

**Claude Code 작업:**
- `guestbook.html` — 이름칸 / 메시지칸 / "남기기" 버튼 / 최신순 목록(이름·메시지·시각)
- 기록부 화면의 색·글꼴·카드 스타일 재사용. **테마 변수는 세 스코프(밝음·시스템 어두움·직접 어두움)에 모두 정의** (`HANDOFF.md` 5-(2) 함정 — 하나라도 빠지면 한쪽에서 조용히 깨진다)
- 빈 메시지면 버튼 비활성화, 글자 수 표시, 전송 중 상태, 오류 메시지 처리
- 메인 화면 푸터에 "방명록" 링크 추가 (옆 버튼들과 같은 모양이 되도록 스타일 맞춤)

---

## 3. 막힌 곳과 확인

### 문제 두 개 발견 → 수정

**(1) `/guestbook` 이 404**
- 원인: Vercel 정적 배포는 기본적으로 `.html` 을 붙여야 열린다 (`/guestbook.html` 만 200)
- 조치: `vercel.json` 에 `cleanUrls: true` 추가

**(2) 방명록 서버가 환경변수를 못 찾음 (503)**
- 원인: **`.env.local` 은 Next.js 가 읽는 방식**이다. `vercel dev` 는 링크된 프로젝트에 등록된 값을 읽는다. 지난주 동기화 기능이 잘 돌아간 건 Upstash 통합이 값을 Vercel 프로젝트에 등록해 뒀기 때문이었다.
- 조치: `vercel env add` 로 **Development 환경에 등록** 후 개발 서버 재시작 → 정상
- 배포용(Production/Preview) 등록은 프롬프트 의도대로 마지막 단계로 남겨 둠

**덤으로 고친 것:** 서비스 워커가 오프라인일 때 **어느 페이지를 요청해도 기록부 화면으로 되돌아가던** 동작을, 그 페이지의 사본을 먼저 찾도록 수정

### 사용자 직접 확인

```
글이 목록에 떴어
```

- 사용자가 로컬에서 직접 이름·메시지를 넣고 "남기기" → 목록에 표시됨
- 데이터베이스에서도 확인: `김 — 안녕하세요` 1건 저장됨
- 즉 **읽기(SELECT)와 쓰기(INSERT) 모두 정상**이고, RLS 정책이 이 둘을 허용하고 있음이 실증됨

**의도적으로 하지 않은 검증:**
수정·삭제가 막혀 있는지는 **확인하지 않았다.** 확인하려면 실제 글을 지우거나 고쳐 봐야 하는데, 정책이 잘못 걸려 있으면 **사용자의 글이 진짜로 지워진다.** 사용자 데이터를 걸고 시험할 일이 아니라 판단해, 마지막 RLS 확인 단계로 넘겼다.

---

## 4. 올리기 · 배포 · 문단속 확인

### 올리기 전 세 겹 점검

| 검사 | 방법 | 결과 |
|---|---|---|
| 올릴 목록에 열쇠가 있나 | `git status` | 없음 |
| 이미 추적 중인 파일에 있나 | `git ls-files` | 없음 |
| **과거 이력에 올라간 적 있나** | `git log --all --name-only` | **없음** |
| 올릴 내용에 키 값이 섞였나 | staged diff 전문 검색 | 없음 |
| 배포 결과물의 공개 파일에 키가 있나 | `.vercel/output/static` 검색 | 없음 |

- 세 번째 검사가 통과했으므로 **키를 폐기하고 재발급할 필요가 없었다.** (한 번이라도 이력에 올라갔다면 파일을 지워도 이력에는 남으므로 그 키는 폐기 대상이다)
- `vercel build` 통과 및 결과물에 `guestbook.html` / `api/guestbook.func` 포함 확인

**기획 Q&A:**

```
질문: 지난주 문서 2개(개발 로그·사례글)도 같이 올릴까요?
선택지: 오늘 작업만 올리기 (추천) / 문서도 함께 올리기
```
- 선택: **오늘 작업만 올리기** (저장소가 공개라 개인 기록물은 보류)

- 커밋 `e11ed96` 후 push. GitHub API 로 실제 올라간 파일 19개를 조회해 `.env` 계열 / `node_modules` / `.vercel` 부재 재확인

### Vercel 환경변수 등록과 재배포

```
Vercel에 환경변수 등록하는 것도 해줘
```

- Vercel 환경 3칸의 역할을 먼저 설명 (Development = 내 컴퓨터 / Preview = 시험 주소 / Production = 실제 주소)
- `vercel env add` 로 **Production·Preview 에 등록** → 총 6개(2개 × 3환경). 값은 `Encrypted` 로만 표시됨
- **값을 등록해도 이미 배포된 사이트에는 반영되지 않는다**는 점을 설명하고 `vercel deploy --prod` 로 재배포
- 배포 후 확인 — 실제 주소에서 `/guestbook` 200, `/api/guestbook` 200, 데이터베이스 연결 성공, **방문자가 받는 파일(메인·방명록·서비스 워커)에 키 노출 없음**

### RLS 최종 확인

```
supabase 어디서 봐야해?
열어줘
```

- Supabase 대시보드의 두 페이지(Policies / Table Editor)를 브라우저로 직접 열어 주고, 확인할 항목 세 가지를 표로 안내

```
다 맞아
```

- 사용자 확인 결과 — RLS 켜짐 ✓ / 정책 2개(SELECT·INSERT) ✓ / **UPDATE·DELETE 정책 없음** ✓

  정책이 없다는 것이 곧 문단속이다. 규칙에 적히지 않은 행동은 전부 거부된다.

- 이후 알린 사항: 이 방명록은 공개이므로 주소를 아는 누구나 글을 남길 수 있고, RLS 때문에 사용자 본인도 화면에서는 남의 글을 지울 수 없다 (지우려면 Supabase Table Editor 사용)

---

## 커밋 히스토리

| 날짜 | 커밋 | 설명 |
|------|------|------|
| 08/03 | `e11ed96` | 방명록 추가 (Supabase 연동) |

누적 40개 커밋.

---

## 기술 스택

- **Frontend**: 단일 HTML 파일 방식 유지 (`index.html`, `guestbook.html`) — 바닐라 JS, 빌드 단계 없음
- **Backend**: Vercel Functions (`api/guestbook.js`, `api/sync.js`)
- **Database**: Supabase (PostgreSQL) — `@supabase/supabase-js` 2.111.0, 서버 측에서만 사용
- **보안 모델**: anon 키 + RLS (SELECT·INSERT 만 허용, UPDATE·DELETE 정책 없음)
- **설정**: `vercel.json` (`cleanUrls`), 환경변수는 Vercel 3환경에 등록
- **Deployment**: Vercel (GitHub `main` 푸시 시 자동 배포)

---

## 주요 기능

1. **방명록** (`/guestbook`) — 이름·메시지를 남기면 Supabase 에 저장되고 최신순 표시. 이름을 비우면 "익명". 빈 메시지는 화면·서버 양쪽에서 차단
2. **키가 브라우저로 나가지 않는 구조** — 방문자 브라우저가 아니라 서버가 Supabase 에 접속. 배포된 사이트의 공개 파일에서 키가 검출되지 않음을 확인
3. **권한이 제한된 대리인** — 서버 코드도 anon 키만 쓰므로 RLS 를 우회할 수 없다. 코드에 버그가 있어도 데이터를 지우거나 고칠 수 없다

---

## 이번에 배운 것

- `.env.local` 은 **Next.js 가 읽는 규칙**이다. 프레임워크가 없으면 플랫폼(Vercel)에 값을 등록해야 한다.
- `NEXT_PUBLIC_` 은 "브라우저에 노출됨"이라는 **사실 표시**다. 노출되지 않는 값에 붙이면 거짓말이 된다.
- 환경변수는 **등록만으로는 반영되지 않는다.** 재배포해야 한다.
- RLS 는 **정책을 안 만드는 것이 곧 금지**다. UPDATE·DELETE 정책의 부재가 문단속이다.
- 키가 git 이력에 한 번이라도 올라갔다면 파일을 지워도 소용없다. **폐기하고 재발급**해야 한다.
