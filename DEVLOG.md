# 주간 기록부 (My Diary) - 개발 로그

AI 코딩 도구와 함께 진행한 개발 작업 기록입니다.

일주일 일정과 건강 상태를 한 화면에서 기록하고 추이로 보는 대시보드를 만들고,
GitHub와 Vercel에 연결해 실제 서비스로 배포하기까지의 과정입니다.

---

## 2026-07-25 (Day 1)

### 1. 대시보드 요구사항 정하기

```
내 일주일 일정이랑 건강상태 정리하는 대시보드 만들어줘
```

**Claude Code 작업:**
- 일정·건강 데이터를 갖고 있지 않은 상태라 데이터를 어떻게 채울지 먼저 확인
- 선택지를 제시하는 과정에서 항목이 세 차례에 걸쳐 확정됨

**기획 Q&A (AskUserQuestion):**

> **Q. 일정과 건강 데이터를 어떻게 채울까요?**
> - 빈 템플릿으로 (추천) / 제가 지금 알려드릴게요 / 기존 파일에서 읽기
>
> **A. 제가 지금 알려드릴게요**

이어서 건강 항목을 묻는 질문에 사용자가 직접 항목을 지정:

```
혈압 당 운동 기분 전체평가
```

```
위에 수면 운동 기분 식사 수분 도 합쳐서 해줘
```

```
너가 위에 말했던 내용들 같이 들어가게
```

- 최종 확정된 기록 항목 **11개**: 수면, 혈압, 공복혈당, 체중, 운동, 물 섭취, 식사(아침·점심·저녁), 복약, 기분, 전체평가, 증상 메모
- 데이터는 브라우저에 저장하는 방식으로 결정 → 사용자가 일정을 미리 알려주지 않아도 화면에서 직접 입력 가능

---

### 2. 대시보드 설계와 구현

**Claude Code 작업:**
- `artifact-design`, `dataviz` 스킬을 불러와 디자인·시각화 기준 확보
- 색상 팔레트를 검증 스크립트로 실제 실행하여 색각 이상 대비 통과 확인 (light/dark 양쪽 전 항목 PASS)
- `index.html` - 의존성 없는 단일 HTML 파일. CSS·JavaScript·SVG 차트 전부 내장 (최종 1,704줄)

**구현한 화면 구성:**
- **요약 타일 10개** — 전체평가 주간 평균을 큰 숫자로, 나머지 지표는 정상/주의/부족 판정과 함께
- **이번 주 일정** — 월~일 원장 형식, 시간과 내용을 넣어 추가하고 체크로 완료 처리
- **생활 습관 격자** — 식사·복약을 칸 클릭으로 바로 체크, 물은 잔 수에 따라 색이 진해지는 방식
- **건강 추이 차트 7종** — 수면·혈압(2계열)·공복혈당·체중·운동·물·기분과 전체평가
- **하루 기록하기** — 날짜 탭을 고르고 전 항목 입력
- **표로 보기** — 차트와 같은 데이터를 표로도 제공

**판정 기준 (일반 권고치 기반):**
- 혈압 — 대한고혈압학회 진료지침 구간
- 공복혈당 — 정상 70–99, 공복혈당장애 100–125, 당뇨 범위 126 이상
- 수면 — 성인 권장 7~9시간 / 운동 — WHO 주 150분 / 물 — 하루 8잔

**검증 방법:**
- 헤드리스 브라우저로 실제 렌더링 스크린샷을 찍어 확인
- 밝은 테마·어두운 테마·좁은 화면(560px) 각각 검증
- 발견한 문제를 즉시 수정: 차트 그리드 마지막 칸이 비는 문제, 시각 입력 칸이 잘리는 문제, 우측 열 여백 문제

---

### 3. GitHub 저장소 연결

```
지금 만든 대시보드랑 https://github.com/joannefavor/My-Diary 깃허브 연결해줘
```

**기획 Q&A (AskUserQuestion):**

> **Q. 대시보드를 My-Diary 레포에 어떤 식으로 연결할까요?**
> - 코드 푸시 + GitHub Pages (추천) / 코드만 푸시 / 레포를 비공개로 바꾼 뒤 진행
>
> **A. 레포를 비공개로 바꾼 뒤 진행** — 이어서 `비공개로 해줘`

**Claude Code 작업:**
- 레포 상태 확인: 존재하지만 비어 있고 **공개(PUBLIC)** 상태임을 발견 → 사용자에게 알림
- `gh repo edit --visibility private`로 비공개 전환
- `README.md` 작성 — 기록 항목표, 화면 구성, 판정 기준 출처, 데이터 저장 방식 설명
- 첫 커밋 후 `main` 브랜치에 푸시
- 비공개 레포에서는 무료 플랜으로 GitHub Pages를 쓸 수 없다는 점을 함께 안내

---

### 4. Vercel 배포

```
버셀에 배포했는데 주소가 어떻게 되는지 알 수 있어?
```

**Claude Code 작업:**
- Vercel CLI로 계정·팀·프로젝트 목록 조회 → **배포가 존재하지 않음**을 확인하고 그대로 보고
- 사용자가 팀 주소를 알려준 뒤 재조회했지만 결과 동일 (가장 최근 배포가 7일 전 다른 프로젝트)
- 배포되지 않은 원인 후보를 정리: 다른 계정 / 임포트 중단 / 비공개 레포 접근 권한 미허용

**기획 Q&A (AskUserQuestion):**

> **Q. 지금 제가 Vercel에 배포해드릴까요?**
>
> **A. 배포 전에 암호 보호 여부 먼저 알려줘**

- 팀 플랜을 API로 조회 → **hobby** 확인
- Vercel 공식 문서를 조회해 플랜별 보호 기능을 표로 정리
  - 암호 보호: Enterprise 전용, Pro는 월 $150 애드온 → **hobby 불가**
  - 프로덕션 도메인 잠금: Pro 이상 → **hobby 불가**
  - 무료 우회로: 프리뷰 배포는 Vercel 로그인이 필요한 보호 상태

> **Q. 이 상황에서 어떤 방식으로 진행할까요?**
> - 프리뷰로 배포(무료 보호) / 프로덕션으로 배포(공개) / 배포하지 않음
>
> **A. 프로덕션으로 배포 (공개)**

- 프로덕션 배포 실행 → **https://my-diary-six-sigma.vercel.app**
- HTTP 200 응답과 페이지 제목까지 확인

---

### 5. GitHub ↔ Vercel 자동 배포 연결

```
그 연결도 도와줘
```

**Claude Code 작업:**
- Vercel API로 프로젝트와 GitHub 저장소를 연결 (프로덕션 브랜치 `main`)
- 비공개 레포였지만 Vercel GitHub 앱이 이미 접근 권한을 갖고 있어 추가 승인 없이 연결됨
- **실제 git 배포를 돌려 검증** — Vercel이 GitHub에서 커밋을 직접 받아와 빌드하고 `READY` 상태로 종료
- 이후 `main`에 푸시하면 사이트가 자동 갱신되는 상태 완성

---

### 6. 예시 데이터 제거

```
예시 데이터 지우고 이번 주 내 일정으로 채워줘
```

**Claude Code 작업:**
- 시작 시 예시를 채우지 않도록 변경, 예시 안내 배너 제거
- 저장 키를 `v3`으로 올려 이미 열어본 브라우저에서도 빈 상태로 시작하게 처리
- 푸터에 `전체 지우기` 버튼 추가 (확인 창 후 삭제)
- 빈 상태에서 발견한 문제 두 가지를 함께 수정
  - 기록이 없는데 운동이 `0분 부족`으로 잘못 판정되던 것 → `기록 없음`
  - 값이 없는 차트가 0~1 축을 그리던 것 → `아직 기록이 없습니다` 안내로 교체
- **일정 채우기는 진행하지 못함** — 사용자의 실제 일정을 알 수 없어 지어내지 않고 비워둔 뒤 요청

---

## 2026-07-26 (Day 2)

### 7. 수면 입력 방식 개선

```
수면시간 기록은 몇 시에서 몇 시까지 자서 몇 시간 잤는지 계산되도록 바꾸고 싶어
```

**Claude Code 작업:**
- 숫자 한 칸(7.5시간) → 취침 시각 + 기상 시각 두 칸으로 변경, 잔 시간은 자동 계산
- **자정을 넘긴 경우 처리** — 23:30에 자서 07:00에 일어나면 7시간 30분으로 계산
- 계산 로직을 8가지 경우로 단위 테스트하여 전부 통과 확인
  - 자정 넘김 / 새벽 취침(01:00→08:00) / 정각 자정 / 분 단위(21:45→05:20) / 한쪽만 입력 / 취침·기상 동일
- 차트 툴팁과 표에 취침–기상 구간을 함께 표시
- 예전 숫자 형식으로 저장된 기록도 그대로 읽히도록 호환 처리
- 시각 입력 두 칸이 한 열에 안 들어가 잘리는 문제를 발견해 칸 폭을 두 열로 확장
- 기록 기준을 명시: **그날 아침에 일어난 잠**을 그날에 적음

---

### 8. 기록하지 않은 항목 처리 방식 변경

```
하루 기록하기 할 때 기록을 넣지 않으면 측정하지 않고 운동을 하지 않은 거니까
그냥 기록 없이 빈칸으로 남게 바꿔줄래?
```

**Claude Code 작업:**
- 요약 타일이 항목마다 **그 항목을 실제로 적은 날만** 분모로 삼도록 계산 방식 변경
- 운동 — 기록이 하나도 없는 주는 판정하지 않음. 단 `0분`으로 직접 넣은 날은 "운동 안 함" 기록으로 인정
- 식사 — 끼니를 하나라도 체크한 날만 분모로 사용
- 복약 — 이행률(%)을 없애고 **복용한 날 수**로 변경. 체크 안 한 날이 안 먹은 건지 안 적은 건지 구분할 수 없기 때문
- 두 가지 경우로 검증: 수면만 기록한 주는 나머지 전부 `기록 없음`, 운동 3일(60/0/90분) 기록 시 `150분 권고 충족 2일 운동`

---

### 9. Claude Code 설정 점검 (/doctor)

```
[슬래시 명령] /doctor
```

**Claude Code 작업:**
- 설치 상태·확장 사용량·메모리 파일·훅 속도·버전을 읽기 전용으로 점검
- 50개 세션(28일치, 8,094줄)을 스캔해 사용 이력 집계
- 발견: 플러그인이 이미 제공하는 스킬을 사용자가 수동 복사해 **중복 등록**된 상태 (md5 동일 확인)
- 발견: `permissions.defaultMode` 미설정, 백그라운드 자동 업데이트 꺼짐
- 훅 2,300여 회 실행 기록은 전부 정상 속도(중앙값 103~332ms, 타임아웃 0건)
- 적용: 중복 스킬 끄기, 기본 모드 `auto`, 자동 업데이트 재활성화, 오래된 초안 파일 정리 (모두 백업 후)

---

### 10. 작업 자료 이전

```
여기에서 이번 프로젝트 관련 만든 자료들을 /Users/yeon/gpters/health_care 여기로 이동해줘
```

**Claude Code 작업:**
- 임시 작업 폴더에 있던 자료를 프로젝트 폴더로 이전
- git 저장소를 통째로 옮겨 **커밋 히스토리와 원격 저장소 연결 유지**
- Vercel 프로젝트 링크(`.vercel`)도 함께 옮겨 해당 폴더에서 배포 명령이 바로 동작하도록 구성
- 작업 중 만든 검증 스크린샷 25개를 `screenshots/`로 정리
- `.gitignore`에 `.vercel`, `.omc`, `screenshots/` 추가 → Vercel 링크가 실수로 공개 저장소에 올라가지 않도록 차단
- 옮긴 파일이 실제로 열리는지 브라우저 렌더링까지 확인

```
응 커밋해줘
```

- `.gitignore` 변경을 커밋·푸시하고, 자동 배포가 정상 동작하는지 재확인 (HTTP 200)

---

### 11. 이전 폴더에서 작업 이어받기 확인

새 폴더에서 별도 세션을 열어 자료가 온전한지 확인했습니다.

```
작업한거 잘 있어?
```

```
최신 커밋을 불러줘
```

**Claude Code 작업:**
- 새 위치에서 git 히스토리·원격 연결·최신 커밋이 모두 살아 있음을 확인


### 12. 끼니마다 무엇을 먹었는지 적는 칸

```
하루기록하기에서 아침 점심 저녁 먹은것만 체크하도록 되어있는데
식단을 넣을 수 있는 칸을 만들어줘
```

**Claude Code 작업:**
- 아침·점심·저녁 각각에 먹은 음식을 적는 입력칸 추가
- 식단을 적으면 그 끼니가 자동으로 체크되도록 연결

---

### 13. 먹는 약 등록과 복용 체크

```
복약도 내가 먹을약 설정할 수 있고 체크박스 넣어서
먹었는지 체크할 수 있게 바꿀수 있을까?
```

**Claude Code 작업:**
- 먹는 약 이름을 미리 등록해 두고, 날마다 하나씩 체크하는 방식으로 변경
- 등록 전에 `복용함`으로 적어둔 날은 그대로 복용한 날로 인정

---

## 2026-07-30 (Day 3)

### 14. 수면 입력을 오전·오후 선택과 5분 단위로

```
건강 대시보드 잠자는 시간 설정을 위에 오전 아래 오후가 들어가게 해주고
시간을 5분 단위로 저장하게
```

**Claude Code 작업:**
- 시각 입력을 `오전/오후 · 시 · 분` 드롭다운으로 교체 (오전이 위, 오후가 아래)
- 분은 5분 단위로만 선택. 기존 값이 5분 배수가 아니면 가까운 눈금으로 표시
- 저장 형식은 24시간 `HH:MM` 그대로라 계산·차트·표는 손대지 않음
- 12↔24시간 변환을 8가지 경우로 검증 (오전 12시=자정, 오후 12시=정오 포함)

---

### 15. 중복 정의된 함수 정리

```
정리해줘
```

**Claude Code 작업:**
- 함수 89개를 훑어 중복 선언을 찾음 → `toMin`(시각 계산)이 수면용·일정용으로 두 번 정의된 상태
- 나중에 한쪽만 고치면 다른 쪽이 조용히 바뀌는 위험이 있어 하나로 합침
- 입력 11가지로 결과가 이전과 같은 것을 확인

---

### 16. 취침 시각과 혈압이 저장되지 않던 문제

```
취침시간 저장이 안되는데?
```

**Claude Code 작업:**
- 원인: 드롭다운을 하나 고를 때마다 입력 폼 전체를 다시 그려서, 고르던 중간 상태가 지워짐
- 오전만 고른 상태에서는 저장할 값이 없어 폼이 빈 상태로 되돌아가는 구조였음
- **같은 원인으로 혈압도 망가져 있던 것을 회귀 검사 중 발견** — 수축기를 치고 다음 칸으로 넘어가면 숫자가 사라짐
- 입력 폼을 뺀 나머지 화면만 갱신하는 경로를 만들어 해결

---

### 17. 입력하지 않은 칸을 빈칸으로

```
그리고 혈당 혈압 체중 운동 기본값은 입력 안 했을때는 그냥 빈칸으로 남겨놓고싶어
```

**Claude Code 작업:**
- 회색 예시 숫자(120, 80, 95, 64.5, 30)가 이미 입력된 값처럼 보여 제거
- 이어서 물 섭취 칸의 예시도 제거
- 단위와 설명은 라벨에 남겨 무엇을 넣는 칸인지는 유지

---

### 18. 끼니별 복약 기록

```
복약은 식단 옆에 있는데 먹은거 옆에 나란히 칸을 만들어서
아침 점심 저녁에 먹은 약을 기록할 수 있게 바꿔줄래?
```

**Claude Code 작업:**
- 식단 칸 오른쪽에 `그 끼니에 먹은 약` 칸을 나란히 배치
- 약은 끼니를 걸러도 먹을 수 있으므로 식사 체크를 자동으로 켜지 않음
- 요약 타일·표에 끼니별 기록 반영
- 이어서 약 등록 블록을 없애고 그 자리를 끼니 칸이 쓰도록 확장 (쓰이지 않게 된 코드 83줄 삭제)

---

### 19. 하루 기록 배치 정리와 컨디션 추가

```
기분을 아래로 내리고 기분 전체평가 순서로 배열해줘
```

```
기분 옆에 컨디션 체크 넣어주고 증상메모칸을 남은 공간에 맞게 만들어줘
```

**Claude Code 작업:**
- 식사 칸이 한 줄을 다 쓰게 해서 다음 줄이 `기분 → 전체평가` 순서로 시작하게 정리
- 컨디션 5점 척도를 기분 옆에 추가, 증상·메모가 같은 줄의 남은 폭을 차지
- 컨디션을 차트·요약 타일·표에 모두 연결 (기록만 되고 사라지지 않도록)
- 차트 색은 색각 이상에서도 구분되는 조합으로 선택

---

### 20. 화면 정리 — 건강 추이 한 줄, 패널 정리

```
건강추이는 3줄로 나와있잖아? 작게 한줄로 보여도 될 거 같아
```

```
일정 아래 생활습관이랑 이번주 증상 메모 체크 보이는건 안 보이게 해줘
```

**Claude Code 작업:**
- 건강 추이 차트 7개를 한 줄에 나란히 배치 (카드 약 164px)
- 좁은 칸에서 축 글자가 뭉치므로 빼고, 최근값을 제목 아래 크게 표시
- 5점 척도 차트는 막대 21개가 뭉쳐 보여 좁은 화면에서는 선으로 전환
- 생활 습관 격자와 이번 주 증상·메모 패널 제거, 관련 코드 194줄 정리

---

### 21. 일정 간격 축소

```
이번주 일정이 너무 길게 차지하고 있어서 칸마다 간격을 줄여줘
일정이 한페이지에 들어올 정도로
```

**Claude Code 작업:**
- 시간당 높이 44px → 30px, 캘린더 안쪽 스크롤 제거
- 칸이 낮아진 만큼 한 시간짜리 일정도 시간과 내용을 한 줄로 표시
- 결과: 일정 패널 765px → 625px, 문서 전체 2238px → 2098px

---

### 22. GitHub 자동 배포 복구

```
깃허브 자동배포 고쳐줘
```

**Claude Code 작업:**
- 연결 정보·설정은 모두 정상이었고, Vercel이 GitHub에서 코드를 직접 받아오는 것도 성공
- 따라서 원인을 **푸시 알림(웹훅) 미전달**로 좁힘
- git 연결을 끊었다 다시 맺어 구독 갱신
- 확인용 커밋을 푸시해 약 2분 뒤 자동 배포가 생기는 것을 검증

---

### 23. Event 줄과 날짜별 Todo

```
일정을 등록할때 날 아래에서 시간 미정을 Event 로 바꿔주고
그 아래 Todo 리스트 5칸정도 체크박스 체크할 수 있게 만들어 줄래?
이것까지 합쳐서 한페이지에 보이게 사이즈 조정해줘
```

**Claude Code 작업:**
- `시간 미정` → `Event`로 이름 변경, 요일마다 직접 입력할 수 있는 칸 추가
- 그 아래 Todo 줄 추가 — 요일별 체크박스 5칸, 내용과 체크 상태를 날짜별로 저장
- 할 일은 요약·차트에 영향이 없으므로 저장만 하고 화면을 다시 그리지 않음 (타이핑 중 글자가 날아가지 않게)
- 체크박스와 입력칸을 `label`로 묶지 않아 입력칸을 눌러도 체크가 바뀌지 않게 처리
- 시간당 높이를 24px로 더 줄여, Todo 줄을 넣고도 패널이 570px로 오히려 작아짐
- Event 줄 높이는 타임라인 한 시간 칸과 같게 맞춤

---

### 24. 구글 캘린더식 일정 등록과 색상

```
일정을 저장할때 시간을 클릭해서 입력하면 일정을 등록할 수 있게 바꾸고 싶어
구글캘린더랑 같은 방식으로 입력하고 색도 바꿀수 있게 할 수 있어?
```

**Claude Code 작업:**
- 빈 시간을 클릭하면 그 자리에 입력 카드가 뜨고, 누른 시각을 30분 단위로 잡아 채움
- 기존 일정을 클릭하면 같은 카드로 제목·시간·색을 고치거나 삭제
- 엔터로 저장, Esc나 바깥 클릭으로 닫힘
- 타임라인 범위를 새벽 5시~자정으로 고정

---

### 25. 구글 캘린더 팔레트 24색 적용

```
[구글 캘린더 화면 캡처] 이거 분석해줄래?
```

```
일정 시간을 선택하면 네모칸이 기본색이 선택돼서
색도 다양하게 구글캘린더랑 같은 색으로다 넣어줘
```

**Claude Code 작업:**
- 보내주신 화면을 캔버스에 올려 **스와치 24개의 픽셀값을 직접 추출** (짐작하지 않음)
- 구글 공식 명명색과 1씩 어긋난 값은 공식 값으로 보정 (Tomato, Flamingo, Banana, Sage, Basil, Peacock, Blueberry, Lavender, Grape)
- 색마다 글자색을 대비로 계산해 지정 — 24색 전부 WCAG AA 통과, 최저 4.92:1
- 6색 시절에 저장된 일정은 새 팔레트의 가까운 색으로 이전

---

## 2026-07-31 (Day 4)

### 26. 클릭한 칸이 기본색으로 채워지게

```
달력 시간을 눌렀을때 그 칸이 선택돼고 기본색상으로 그 칸이 채워지고
내용을 적게 하고싶은데 적용이 안 됐네
```

**Claude Code 작업:**
- 원인: 입력 카드를 여는 함수가 시작할 때 이전 카드를 정리하면서, 방금 만든 미리보기 칸까지 지우고 있었음
- 카드 안에서 미리보기 칸을 만들도록 순서를 바꿔 해결
- 클릭한 자리에 기본색 칸이 생기고 `(제목 없음)`으로 표시
- 제목을 치면 칸 안 글자가, 색을 고르면 칸 색이 바로 바뀜
- 시작·종료 시각을 고치면 칸 위치와 높이도 따라 움직임
- **검증 중 실제 버그를 하나 더 발견해 수정** — 창 크기가 바뀌면 입력 카드를 닫도록 되어 있어, 휴대폰에서 키보드가 올라오거나 화면을 돌리면 적던 내용이 사라지는 상태였음. 닫지 않고 자리만 다시 잡도록 변경


## 2026-08-11 (Day 5)

> 8월 1일부터 8월 8일까지의 작업(기기 간 동기화, 홈 화면 앱, 방명록, 오늘의 말씀)은
> 기능별로 나눈 별도 문서에 있습니다 — `DEVLOG_기기간동기화와홈화면앱.md`,
> `DEVLOG_방명록Supabase연동.md`, `AI_CASE_STUDY_오늘의말씀365일.md`.

### 27. 동기화가 빈 칸을 '적은 것'으로 세어 기록을 덮던 문제 수정

**Claude Code 작업:**
- 화면을 열기만 해도 오늘 날짜의 **빈 칸이 만들어진다**. 폼을 그리려면 필요한 것이지
  사람이 적은 게 아닌데, 합치기가 이걸 '적은 것'으로 셌다
- 그래서 처음 연결하는 기기에서 충돌로 판정되고, "이 기기 것을 남긴다" 규칙에 따라
  **빈 칸이 남았다.** 받지 못하는 데서 끝나지 않고 그 빈 칸을 올려 저쪽 기록까지 덮었다
- 내용 없는 하루를 가려내어, 합칠 때 '그 날짜가 없는 것'과 같게 다루도록 수정
- 지운 날도 빈 칸이 되므로 **지우기가 전해지는 동작은 그대로** 유지
- 만들 때 45가지를 검사했지만 "두 번째 기기에서 실제로 받아보는" 경우가 빠져 있었다.
  그 경우를 검사에 추가 — 빈 칸 판정 8가지, 처음 연결 4가지, 되돌이 10가지 통과

---

### 28. 기능별 개발 로그와 사례글 정리

**Claude Code 작업:**
- 한 파일에 몰아쓰지 않고 기능 단위로 문서를 나눔
- 기기 간 동기화와 홈 화면 앱 / 방명록 Supabase 연동 — 각각 개발 로그 + 사례글
- 오늘의 말씀 365일 — 사례글 작성
  - 카톡 대화 내보내기에서 327일을 날짜별로 정리하고, 빠진 38일은 책 사진에서
    옮겨 적어 채운 과정
  - 원문이 「주님의 음성」(규장) 책이라 개인 묵상용임을 글에 밝힘

---

### 29. 하루 기록하기에서 날짜 이동

```
하루기록하기에서 < > <하루기록하기> 로 날짝 이동할 수 있도록 바꿔줄래?
```

**Claude Code 작업:**
- 머리글을 `< 하루 기록하기 >` 로 바꾸고 하루씩 이동
- **주를 벗어나면 요약·차트가 가리키는 주도 함께 옮긴다.** 안 그러면 지난주 요약을
  보면서 이번주 기록을 적는 상태가 된다
- 같은 주 안에서는 탭과 입력 폼만 다시 그려 화면이 덜 흔들리게 함
- 요일 탭은 그대로 두어 주 안에서는 바로 고를 수도 있음

---

### 30. Todo 첫 칸 QT 고정 · 상단 주 이동 · 건강 추이 기간 선택

```
Todo에서 제일 위에칸은 QT 라고 고정으로 넣어주고 했는지 체크만 하게 바꿔주고
맨 위 주간기록부 옆에 날짜 나오는 부분도 일주씩 날짜 이동하도록 < > 사용해서 만들어줘
건강추이도 주,월,연도,검색일 설정 해서 움직이도록 바꿔줄래?
```

**Claude Code 작업 — 세 가지:**

**(1) Todo 첫 칸을 QT 로 고정**
- 이름은 못 바꾸고 했는지만 체크. 나머지 네 칸은 그대로 자유 입력
- 체크하면 이름도 함께 저장해 표·집계에서 보이게 함

**(2) 맨 위 머리글에 `< >` 로 한 주씩 이동**
- 요약·차트·일정이 함께 따라간다

**(3) 건강 추이 기간 선택 — 주 / 월 / 연도 / 기간 지정**
- 주·월·기간 지정은 하루가 한 점, **연도는 한 달이 한 점(그 달 평균)**
- 점 개수가 7로 박혀 있어서 좌표·축·마우스 영역·풍선·표를 전부 데이터 개수에
  맞추도록 고침
- 점이 많으면 축 라벨을 솎아내고, 연도 보기에서는 풍선과 표가 `8월`처럼 월 단위로 표시
- 고른 기간은 저장되어 다시 열어도 유지
- 검증: 주 7점 / 월 31점 / 연도 12점 / 기간 지정 15점(6-10~6-24)

**함께 고친 것:** 기간 지정 날짜 칸이 `주`·`월`을 골라도 계속 보였다.
CSS의 `display: flex` 가 `hidden` 속성을 이기고 있었다.

---

## 2026-08-13 (Day 6)

### 31. 방명록 링크를 푸터에서 감춤

```
대시보드 열어줘
```

**Claude Code 작업:**

`vercel dev` 로 띄워 확인하다가 방명록만 502 로 뜨는 것을 발견했다.
원인을 따라가 보니 **코드 문제가 아니라 외부 리소스가 사라진 것**이었다.

- `.env.local` 의 `SUPABASE_URL` 호스트가 DNS 에서 아예 조회되지 않는다(NXDOMAIN).
  Supabase 무료 프로젝트가 비활성·삭제되면 이렇게 된다
- 네트워크 문제와 헷갈리기 쉬운데, 같은 환경에서 Upstash(401)와 외부 HTTPS(200)는
  정상적으로 붙는 것을 확인해 **Supabase 쪽만 죽은 것**으로 좁혔다

**키를 새로 발급받을지 물었더니 "링크만 숨기자"로 정리됐다.** Supabase 를 쓰는 곳이
`api/guestbook.js` 하나뿐이라 나머지 기능은 영향이 없다 — 동기화는 Upstash 를 쓴다.

- 푸터의 방명록 링크를 **지우지 않고 주석으로 감쌌다.** 왜 감췄는지와 되살리는 방법을
  주석에 함께 적어두었다
- `guestbook.html` 과 `api/guestbook.js` 는 그대로 둠. 새 Supabase 프로젝트를 만들고
  `SUPABASE_URL`·`SUPABASE_ANON_KEY` 만 바꾼 뒤 주석을 풀면 복구된다
- 사례글까지 쓴 기능이라 **지우는 대신 감추는 쪽**을 골랐다

배포한 주소의 실제 응답을 받아 푸터에 링크가 없는 것까지 확인했다.
(서비스워커 캐시 때문에 이미 쓰던 기기에서는 새로고침이 한두 번 필요하다)

---

## 2026-08-17 (Day 7)

> **커밋 이름과 내용이 어긋난 곳이 있다.** 아래 32~34번 작업은 따로 커밋되지 않고
> `ae986f0` "사이트에 작업 기록 페이지 추가" 에 함께 딸려 들어갔다.
> 그 커밋의 `index.html` +594줄이 사실상 이 세 가지다. `docs.html` 추가와는 무관하다.
> 이미 올라간 뒤에 발견해 역사를 고치지 않고 여기에 적어 둔다.

### 32. 여덟 페이지로 나누고 왼쪽에 이동 막대

```
페이지는 1.메인화면 2.일정 3.업무 4.신앙 5.건강 6.나의역사 7.미팅 8.지식창고 으로
나눠서 페이지 바뀌게 만들고 싶고 메인페이지 좌측 상단에 페이지 이동할 수 있게 탭 만들어줘
```

**Claude Code 작업:**

- 주소의 해시로 페이지를 가른다 (`#/`, `#/schedule`, `#/health` …). 새로고침해도
  보던 페이지가 그대로고 뒤로 가기도 동작한다. 모르는 주소는 메인으로 보낸다
- 페이지 목록을 `PAGES` 배열 하나에 두고 탭을 거기서 그린다. 이름·순서는 배열만 고치면 된다
- **감출 때 `hidden` 속성 대신 `.is-off` 클래스를 쓴다.** 말씀 패널과 동기화 패널은
  각자 `hidden` 을 켜고 끄는 규칙이 이미 있어서, 같은 속성을 건드리면 서로 싸운다
- 동기화 패널에는 일부러 `data-page` 를 안 붙였다. 어느 페이지에서 열어도 보인다
- `data-page="main faith"` 처럼 이름을 여러 개 적으면 그 페이지들에서 함께 보인다.
  패널을 복사하지 않으므로 두 곳의 상태가 어긋나지 않는다 — 말씀과 하루 기록이 이렇게 걸쳐 있다
- 이동 막대는 처음에 위쪽 가로줄이었다가 왼쪽 세로로 옮겼고, 첫 버튼을 제목 아래
  밑줄에 맞췄다(78px). 760px 아래에서는 다시 가로줄로 눕는다

업무·나의역사·미팅·지식창고는 저장할 곳이 없어 빈 틀만 두었다.

---

### 33. 빠른 기록 — 한 줄로 적으면 알아서 들어가게

```
하루기록하기를 그 양식 말고 이런식으로 넣어서 짧게 만들 수 있을까?
(Quick Capture 화면 예시와 함께)
```

**Claude Code 작업:**

`혈압 120/80 물 5잔 기분 4 아침 현미밥` 처럼 한 줄에 몰아 적으면 제자리로 나눠 넣는다.

- **해석은 전부 브라우저 안 규칙으로 한다.** 적은 글이 바깥으로 나가지 않고 의존성도 안 늘었다
- **알아들은 말만 지우고 나머지는 칸에 남긴다.** 넣은 것을 그대로 되짚어 보여주므로
  엉뚱한 곳에 들어갔는지 바로 보인다
- 범위를 벗어난 값(`혈압 999/80`, `기분 9`)은 넣지 않고 모르는 말로 남긴다.
  오타가 조용히 저장되는 쪽이 더 나쁘다
- 수면을 운동보다 먼저 본다. 안 그러면 "분"·"시간" 조각을 운동 규칙이 집어간다
- 식사·복약은 뒤에 오는 말을 통째로 가져가므로 다른 규칙을 모두 지나온 뒤에 본다

**한계:** `7시간 잤다` 처럼 길이만 적으면 취침·기상 시각을 알 수 없어 폼의 드롭다운은
빈 칸으로 남는다. 요약·차트에는 반영된다. 시각까지 남기려면 `취침 23시 기상 6시`.

검증: 파서 규칙 25가지를 뽑아내 node 로 돌려 전부 통과.

---

### 34. 빠른 기록에 일정과 업무 붙이기

```
일정이나 업무 신앙 나의 역사 미팅들도 빠른 기록으로 남기게 하려면 어떻게 하는게 좋을까?
```

**저장할 곳이 있는 둘만 먼저 했다.** 일정은 `d.schedule[]`, 업무는 이미 있던 요일별
Todo 칸을 쓴다. 신앙·나의역사·미팅·지식창고는 무엇을 적을지부터 정해야 해서 남겼다.

- **분류는 추측하지 않는다.** 태그가 없으면 지금 보고 있는 페이지로, `#일정`·`#업무`·`#건강`
  을 붙이면 그쪽으로. `30분` 이 운동인지 회의 길이인지 기계가 알 수 없기 때문이다
- 일정: `금요일 오후 2시 법무회의 1시간` → 8/21 14:00–15:00.
  요일·오늘/내일/모레, `2시~4시` 범위, `30분`·`1시간` 길이를 읽는다. 제목이 없으면 넣지 않는다
- `2시~4시` 처럼 물결표를 붙여 적으면 시각 규칙이 낱말 경계를 못 찾던 것을 고쳤다.
  숫자 사이의 물결표에만 빈칸을 넣는다 — 제목 속 붙임표는 건드리지 않는다
- 업무: QT 로 고정된 첫 칸을 건너뛰고 둘째 칸부터 채운다. 다 차면 넣지 않고 알린다
- **업무 페이지를 채웠다.** "준비 중"이던 자리에 이번 주 업무가 날짜별로 쌓이고 체크된다.
  일정 페이지의 Todo 칸과 같은 자료라 어느 쪽에서 고쳐도 함께 바뀐다.
  적은 것이 안 보이면 기능이 없는 것과 같다

검증: 파서 17가지를 node 로, 화면 동작 9가지를 브라우저에서 실제로 칸에 적고 버튼을 눌러
확인했다 — 달력에 일정이 뜨는지, 업무 목록에 쌓이는지, 체크가 남는지까지. 검사용 임시
페이지는 지웠다.

---

### 35. 미팅 페이지

```
미팅 페이지 채워줘
```

무엇을 적을지부터 정해야 했다. **제목 · 참석자 · 논의 · 할 일**로 골랐다.
미팅의 결과는 보통 누가 무엇을 하느냐로 남고, 그게 나중에 찾아보는 이유이기 때문이다.

- **하루 밑에 저장한다**(`d.meetings[]`). 동기화가 날짜 단위로 합치므로 이렇게 두면
  동기화가 공짜로 따라온다. 최상위에 새 목록을 만들면 합치기 규칙을 새로 짜야 하는데,
  그건 예전에 기록을 덮어버린 적 있는 까다로운 부분이다
- 글자 칸은 **칸을 벗어날 때 저장하고 화면을 다시 그리지 않는다.** 매번 다시 그리면
  적는 도중 커서가 튄다. 체크·추가·삭제처럼 목록 모양이 바뀔 때만 다시 그린다
- 지우기는 적어 둔 것이 있을 때만 묻는다. 빈 칸은 그냥 지운다
- **`id` 로 찾아 지운다.** 목록을 시각 순으로 정렬해 보여주므로 화면 차례와 저장 차례가
  다를 수 있고, 순번으로 지우면 엉뚱한 것이 지워진다

---

### 36. 해시태그를 걷고 넣을 곳을 먼저 고르게

```
해시태그로 기록되도록 되어있는데 어디에 넣을지를 먼저 선택하고 기록에 저장되도록 바꾸고 싶은데
```

- 칸 위에 고르는 단추를 두었다. **고른 곳이 곧 저장될 곳**이다
- 해시태그는 지우지 않고 지름길로 남겼다. **쓰면 고른 자리도 함께 옮겨간다** —
  조용히 다르게 동작하지 않고 어디로 들어갔는지 눈에 보인다
- 고른 곳에 따라 예시 문구가 바뀐다. 무엇을 적으면 되는지 그 자리에서 보인다
- 페이지를 옮기면 고른 것이 그 페이지에 맞춰 초기화된다. 업무 페이지에 와서
  건강이 골라져 있으면 엉뚱한 곳에 들어간다

나중에 **차례를 왼쪽 페이지 막대와 같게** 맞췄다. 두 곳의 차례가 다르면 손이 헷갈린다.

---

### 37. 나의역사 · 지식창고, 그리고 신앙을 셋으로

```
나의 역사 지식창고도 다 그렇게 나올 수 있게
신앙에서 오늘의 말씀 묵상일기 기도 이렇게 내용 적고싶어
```

모양이 같아서 **한 벌로 만들고 이름만 바꿔 쓴다**(`NOTE_KINDS`). 제목 + 내용이다.
나의역사·지식창고·묵상일기·기도 넷이 같은 코드를 쓴다.

- **이번 주로 끊지 않고 적어 둔 것을 전부 보여준다.** 최근 것이 위로.
  업무·미팅은 이번 주만 보는 게 맞지만, 지난주 것이 안 보이는 창고는 창고가 아니다
- 신앙은 말씀(읽기) 아래에 묵상일기와 기도를 나눠 놓았다.
  빠르게 쓰기에서 `신앙` 은 묵상일기로 가고, 기도는 `#기도` 나 그 칸의 버튼으로 넣는다.
  고르는 단추를 여덟 개로 늘리면 페이지 차례와의 짝이 깨지기 때문이다

---

### 38. 메인화면을 오늘 것으로

```
메인 페이지에서 건 전체 평가가 보이는데 그건 건강쪽 맨 위에 보이게 바꿔주고
그 자리에는 오늘의 일정과 할 일이 보이게 해줘
```

- 요약 타일 11종을 건강 페이지 맨 위로 옮겼다
- 그 자리에 **그날의 일정과 할 일**을 나란히 놓았다. 일정 페이지·업무 페이지와
  **같은 자료**라 어디서 체크해도 함께 바뀐다
- 오늘이 아닌 날을 보고 있으면 제목이 '그날'로 바뀌고 "오늘이 아닙니다"라고 알린다.
  어제 일정을 오늘 것으로 착각하지 않게

---

### 39. 지난 일도 적을 수 있게

```
빠른 기록 할때 날짜설정을 할 수 있게 일정에 넣는 방식으로 바꿔줘 지난 일정을 기록 할 수도 있거든
```

- 달력 칸을 넣고 `(수) 68일 전` 처럼 며칠 전인지 알려준다
- 글 안에 적어도 된다 — `8/3`, `8월 3일`, `2025-12-24`, `어제`.
  해를 안 적으면 지금 보고 있는 날의 해로 본다
- **날짜를 옮기면 보고 있는 주도 함께 옮긴다.** 안 그러면 6월 것을 적는데
  요약·차트는 8월을 가리킨 채 남는다
- **헷갈릴 곳을 막았다:** `혈압 90/60` 을 9월 60일로 읽지 않는다(달·일이 말이 되는
  범위일 때만 날짜로 본다). `2월 30일`·`13/5` 처럼 없는 날짜는 날짜로 치지 않고
  제목에 그대로 둔다 — 조용히 3월 2일로 바꿔치기하는 것보다 낫다

---

### 40. 일정 칸에서 시각 글자를 뺌

```
일정등록할때 내가 지정한 시간에 내용을 넣어주면 되고 시간기록은 거기에는 안 되어도 됨
왜냐면 이미 왼쪽에서 시간은 볼 수 있으니깐
```

시간표 칸 안에 `09:00–09:30` 이 제목 위에 한 줄 더 있었다. **왼쪽 눈금이 이미
알려주는 것을 좁은 칸에서 두 번 쓰고 있었고,** 30분짜리 칸은 그 한 줄 때문에
제목이 잘렸다.

- 칸에는 내용만. 정확한 시작·끝은 마우스를 올리면 나온다
- **목록 보기에는 시각을 그대로 두었다.** 거기엔 왼쪽 눈금이 없어서 시각을 빼면
  몇 시 일인지 알 방법이 없어진다
- 시각 표시용 CSS(겹친 칸에서 접던 규칙, 짧은 칸에서 한 줄로 붙이던 규칙)도 함께 지웠다.
  가리킬 대상이 없어졌다

**함께:** '빠른 기록' 을 '빠르게 쓰기' 로 바꾸고, 날짜 칸의 '넣을 날짜' 라는 글자를 뺐다.
화면에서는 지웠지만 화면 낭독기용 이름은 남겼다 — 눈으로는 달력 모양으로 알 수 있지만
소리로 들으면 그냥 '빈 칸'이 된다.

---

### 41. 글씨와 색 바꾸기

```
글씨체랑 색상을 다르게 바꾸고 싶어 어떤 종류가 있을까?
(이어서 복숭아색 견본, 다시 다섯 색 팔레트 이미지)
```

**먼저 제약을 알렸다.** 이 앱은 파일 하나로 돌아가고 외부 요청이 없어서 비행기 모드에서도
열린다. Pretendard 같은 웹폰트를 CDN 으로 불러오면 그 성질이 깨지고, 한글 폰트는 용량이
커서 자체 저장하면 파일이 몇 배가 된다. **기기에 이미 있는 글꼴 조합을 바꾸는 쪽**으로 갔다.

**글씨:** 제목의 세리프를 걷고 한 가지로 통일. 제목은 굵기(700)와 크기로만 층을 만든다.
숫자·시각은 고정폭 그대로라 자리가 흔들리지 않는다.

**색:** 견본 이미지에서 색을 **직접 뽑아냈다.** 화면을 보고 눈대중으로 맞추지 않았다.
처음 받은 한 칸짜리 견본에서 `#ecb390` 을, 이어서 받은 다섯 칸 팔레트에서 아래 다섯을 얻었다.

```
#F6B08A 복숭아   #FFD6B5 연복숭아   #FCEEE3 크림   #B7C8A1 세이지   #3E3A39 먹
```

**역할을 나눠 넣었다.**

| 색 | 자리 |
|---|---|
| `#fceee3` 크림 | 배경 |
| `#fffaf4` (파생) | 카드 — 팔레트에 흰색이 없어 크림보다 한 단계 밝게 만들었다. 안 그러면 카드와 배경이 붙어 보인다 |
| `#f6b08a` 복숭아 | 고른 페이지 탭, 버튼, 전체평가 타일 |
| `#ffd6b5` 연복숭아 | 은은한 강조 바탕 |
| `#b7c8a1` 세이지 | 오늘 표시 |
| `#3e3a39` 먹 | 글자, 복숭아 위 글자 |

세이지에 '오늘'을 준 것은 그때까지 **고른 것**과 **오늘**이 같은 색을 나눠 쓰고 있었기
때문이다. 이제 복숭아는 "내가 고른 것", 세이지는 "오늘"로 뜻이 갈린다.

**강조색을 두 갈래로 나눴다 — 이게 이 작업의 핵심이다.**

- `--accent` `#f6b08a` — 넓게 칠하는 자리. 그 위에는 먹색 글자를 얹는다
- `--accent-ink` `#9e5225` — 글자·초점 테두리·체크 상자처럼 **가는 것**

나누지 않으면 **연한 복숭아색이 크림 배경 위에서 1.6:1** 이라, 그대로 초점 테두리에 쓰면
키보드로 옮겨 다닐 때 지금 어디에 있는지 보이지 않는다. 체크 상자도 마찬가지다 —
브라우저가 흰 체크를 그리는데 바탕이 연하면 체크했는지 알 수 없다.
초점 테두리 15곳과 체크 상자 3곳을 진한 쪽으로 돌렸다. 세이지도 같은 이유로
`--sage` 와 `--sage-ink` 로 나눴다.

**계산해서 찾은 것 — 복숭아와 세이지의 밝기가 1.03:1 로 사실상 같다.**
흑백으로 보거나 색각 이상이 있으면 두 색이 구분되지 않는다. 그래서 세이지를
**색만으로 뜻을 나르는 자리에는 쓰지 않았다.** 오늘 표시에는 날짜 숫자나 `TODAY` 글자가
늘 함께 있어서 색이 안 보여도 뜻이 전달된다. 이 앱이 처음부터 지켜온
"색 단독으로 뜻을 나르지 않는다" 와 같은 원칙이고, 코드 주석에도 남겼다.

**색을 눈대중으로 고르면 흐린 글자가 조용히 안 보이게 된다.** 대비를 계산하는 스크립트를
만들어 밝은·어두운 두 테마의 조합을 매번 검사했다. 미달이 두 번 나왔고 그때마다 색을
조정했다 — 흐린 글자가 연한 칸 위에서 4.36:1, 4.48:1 로 걸렸다.
결과적으로 **예전 회색 테마보다 읽기 쉬워졌다** (흐린 글자 3.6:1 → 5.1:1).

**그대로 둔 것:** 차트 3색. 처음 만들 때 색각 이상 대비까지 검증해 통과시킨 값이라
건드리면 그 검증이 무효가 된다. 새 배경 위에서 잘 보이는 것은 확인했다.

**일정 색은 갈아끼우지 않고 덧붙였다.** 파란 칸이 크림빛 화면에서 혼자 겉돌아 새 일정의
기본색을 복숭아로 옮겼는데, 구글 24색 **뒤에** 두 색을 더하는 방식으로 했다.

- `--ev-25` 복숭아 `#f6b08a`, `--ev-26` 세이지 `#b7c8a1` (둘 다 글자는 먹색)
- 앞의 24개는 손대지 않았다. **기존 색을 갈아끼우면 이미 색을 골라 둔 일정들이 어느 날
  갑자기 다른 색으로 바뀐다.** 뒤에 덧붙이면 그럴 일이 없다
- 새 두 색도 글자 대비를 계산해 확인했다 (6.15:1, 7.18:1). 기존 24색은 검정 아니면 흰색
  글자를 쓰지만 이 둘은 화면 전체와 같은 먹색을 쓴다

**놓치기 쉬운 곳:** 휴대폰 주소 표시줄 색(`theme-color`)도 새 배경과 맞췄다.
안 고치면 앱 위쪽에만 예전 회색 띠가 남는다.

**색 작업은 세 번에 걸쳐 이뤄졌다** — 복숭아 한 색(`13f8ec0`) → 다섯 색 팔레트
(`4e234c9`) → 일정 기본색(`e70b4f1`). 매번 대비를 다시 계산했고, 그때마다 미달이
나와 색을 조정했다.

---

### 42. 병원 방문 기록, 그리고 늘어나도 길어지지 않게

```
건강에는 병원 방문 기록을 넣고 병원이름 방문날짜 내용을 넣고
나중에 병원별 날짜별로 볼 수 있게 하고 싶어
```

병원 이름·방문일·내용을 적는다. **하루 밑에 저장한다**(`d.visits[]`) — 방문일이 곧
저장 위치다. 그래서 달력 칸에서 날짜를 바꾸면 그 날짜 밑으로 옮겨지고, 동기화도
날짜 단위로 그대로 따라온다.

빠르게 쓰기에서는 `연세내과 #병원`, 지난 것은 `7/20 강남치과 #병원`.
**태그로만 받는다** — 고르는 단추를 여덟 개로 늘리면 왼쪽 페이지 막대와 차례가
어긋나기 때문이다.

```
병원이 늘어나면 페이지가 계속 늘어나서 페이지가 길어지는데
늘어나지 않고 조회해서 볼 수 있게 바꾸려면 어떤 방법이 좋을까?
```

카드 하나가 세 칸이라 열 번만 가도 화면이 길어졌다. **한 줄 목록 + 눌러 펼치기**로 바꿨다.

- 평소엔 `08.17 · 연세내과 · 혈압약 3개월 처방` 한 줄. 누른 것만 펼쳐진다
- **한 번에 하나만 펼친다.** 여러 개를 열어두면 결국 다시 길어진다
- 조회 세 가지: 병원 고르기(적어 둔 이름이 자동으로 쌓인다), 찾기 칸(이름과 내용을
  함께 뒤진다), 날짜별·병원별 보기. 오른쪽에 `5번` / 걸렀을 때 `2 / 4번`

**적는 도중에 화면을 다시 그리지 않는다.** 대신 위의 한 줄 요약만 그 자리에서 고쳐 쓴다.
통째로 다시 그리면 적던 칸이 접혀버린다. 이 앱에서 세 번째로 만나는 같은 문제다 —
16번(취침 시각), 35번(미팅)과 뿌리가 같다.

`+ 새로 적기`는 병원 고르기와 찾기를 **비우고** 새 줄을 펼친다. 안 그러면 방금 만든
빈 기록이 조건에 안 걸려 화면에서 사라진다.

**함께 손본 것**

```
일정 event 는 체크박스는 없어도 될 거 같고 오늘 오른쪽 끝에 날짜는
아래 빠르게 쓰기 옆쪽 달력 처럼 날짜 선택할 수 있게 바꿔줘
```

- Event 줄(시각 없는 일정)에서 체크상자를 뺐다. 칸이 낮아 글자가 밀렸다.
  **완료 표시가 사라진 것은 아니다** — 오늘 패널과 목록 보기에서 그대로 체크한다
- 오늘 패널 오른쪽 끝을 달력 칸으로 바꿨다. 빠르게 쓰기와 **같은 날짜를 본다** —
  한쪽에서 옮기면 다른 쪽도 따라온다. 따로 놀면 위에서 6월을 보면서 아래에
  8월 기록을 넣게 된다
- 날짜 문구(`(월) 오늘`, `(수) 68일 전`)를 만드는 코드를 `whenWord` 하나로 합쳤다.
  따로 두면 한쪽만 고쳐져 서로 다른 말을 한다

검증: 화면에서 39가지를 눌러 확인했다 — 병원 기록 10, 한 줄 목록과 조회 14,
요약 즉시 갱신 3, Event 체크상자와 오늘 달력 12.

---

### 43. 다른 앱과 비교해 공휴일과 반복 일정을 가져옴

```
이런 앱이 있는데 내 주간기록부랑 비교해서 어떤점을 바꿀 수 있을까?
(qoro todo 화면 사진)
```

**먼저 그 앱이 무엇인지부터 확인했다.** 사진 속 링크 `qoro.ooo` 를 열어보니
할 일 앱이 아니라 **실시간 통역 서비스**였다 — 같은 만든이의 다른 제품을 소개하는
링크였다. 다시 찾아 [qorotodo.ooo](https://qorotodo.ooo/) 를 읽었다.

**두 앱은 뿌리가 달랐다.** qoro todo 는 구글 캘린더·Tasks 에 붙어서 데이터를
**사용자의 구글 계정에** 두고, 그 위에 모임 캘린더·공유 링크·일정 조율 투표를 얹은
협업 도구다. 주간 기록부는 기록이 **브라우저 안에** 있고 혼자 쓰는 기록부다.

그래서 **가져오지 않기로 한 것**을 먼저 정했다.

- **구글 연동** — 회사 일정이 이미 구글에 있다면 큰 장점이지만, "기록이 내
  브라우저에만 있다"는 성질이 깨진다. 혈압·복약·묵상을 적는 곳이라 그 성질이
  기능보다 앞선다
- **공유 링크·모임·투표** — 서버가 필요하고 개인 기록부가 협업 도구로 성격이
  바뀐다. 방명록 하나 붙였다가 Supabase 가 사라져 감춘 적도 있다

가져온 것은 **공휴일**과 **반복 일정** 둘뿐이다. 나머지는 그 앱이 "구글 위에 얹은
협업 도구"라서 있는 것이지, 이 앱에 없어서 아쉬운 게 아니었다.

**공휴일 — 자료를 두 방법으로 대조했다**

설·추석은 음력이라 외워서 적으면 틀린다. 자료를 받아온 뒤,
**대체공휴일을 규칙(관공서의 공휴일에 관한 규정)으로 직접 계산해 맞대어 봤다.**
2026년 네 건이 정확히 일치했고, 보내주신 사진의 `8/17 쉬는 날 광복절` 과도 맞았다.

- 받아온 2027년 자료에 **`노동절 대체공휴일`·`제헌절 대체공휴일` 이 있었다.**
  둘 다 관공서 공휴일이 아니라 대체공휴일이 생길 수 없다. 계산과 대조해 걸러냈다
- 같은 이유로 제헌절(2008년부터 공휴일 아님)과 근로자의 날은 넣지 않았다
- **모르는 해에는 아무것도 칠하지 않는다.** 틀린 날을 빨갛게 칠하느니 비워 둔다
- 색만으로 알리지 않고 이름을 함께 적는다. 대체공휴일은 이름이 있어야 왜 쉬는지 안다

**반복 일정 — 규칙이 아니라 사본을 미리 넣는다**

`매주 금요일 오후 2시 법무회의` 처럼 적으면 앞으로 것까지 한 번에 들어간다.

그릴 때마다 규칙을 펼치는 방식도 있다. 그러면 데이터는 깔끔하지만 **체크·색·시각
고치기를 다루는 코드를 전부 손봐야 한다.** 사본으로 넣으면 하나하나가 보통 일정과
똑같아서 이미 있는 기능이 그대로 동작한다. 대신 같은 반복끼리 `rid` 로 묶어
"이 반복 전부"를 할 수 있게 했다.

- 한도: 매일 60번, 매주 26번(반년), 매달 12번(1년). 끝없이 만들 수는 없다
- 지울 때 **"전부 지울지, 이 하루만 지울지"를 묻는다.** 예약이 하루 밀려 그 하나만
  지우는 일이 흔하다
- 매달 31일처럼 그 달에 없는 날짜는 건너뛴다. 3월 3일로 밀려 들어가지 않는다
- **한계:** 제목·시각을 고치면 그 하루만 바뀐다. "앞으로 전부 고치기"는 없다

**함께:** 빠르게 쓰기를 메인화면에만 두었다. 업무·미팅 페이지의
"위에서 빠르게 쓰면…" 안내도 고쳤다 — 그 페이지에는 이제 '위'가 없어서
없는 것을 가리키는 안내가 된다.

검증: 공휴일 6가지, 빠르게 쓰기 자리 12가지, 반복 일정 11가지를 화면에서 확인했다.

---

## 커밋 히스토리

| 날짜 | 커밋 | 설명 |
|------|------|------|
| 07/25 15:26 | `4c8bf40` | 주간 기록부 대시보드 추가 |
| 07/25 15:54 | `5b849c9` | 예시 데이터 제거하고 빈 상태로 시작 |
| 07/26 17:00 | `86ac48a` | 수면을 취침·기상 시각 입력으로 바꾸고 시간을 자동 계산 |
| 07/26 17:11 | `9f2889c` | 적지 않은 항목을 0으로 판정하지 않고 빈칸으로 남김 |
| 07/26 17:36 | `b6c1209` | 로컬 작업 폴더용 무시 규칙 추가 |
| 07/26 18:59 | `f542885` | 주간 일정을 캘린더 격자로 바꿈 |
| 07/26 20:21 | `dce4f09` | 배포에 필요 없는 폴더를 업로드에서 뺌 |
| 07/26 20:31 | `b3acaf9` | 끼니마다 무엇을 먹었는지 적는 칸을 넣음 |
| 07/30 00:07 | `0a968f1` | 수면 시각을 오전·오후 선택과 5분 단위로 입력하도록 변경 |
| 07/30 00:13 | `bede312` | 중복 정의된 toMin 함수를 하나로 합침 |
| 07/30 00:32 | `84a3826` | 취침 시각과 혈압이 저장되지 않던 문제 수정 |
| 07/30 00:37 | `006975e` | 혈압·혈당·체중·운동 칸의 예시 숫자를 없앰 |
| 07/30 00:41 | `431f255` | 물 섭취 칸의 예시 숫자도 없앰 |
| 07/30 00:55 | `589147c` | 끼니별로 먹은 약을 적을 수 있게 함 |
| 07/30 07:52 | `dc30898` | 약 등록 블록을 없애고 그 자리를 끼니별 칸이 쓰도록 넓힘 |
| 07/30 07:59 | `11a58d4` | 기분을 아래로 내려 전체평가와 나란히 놓음 |
| 07/30 09:53 | `b7df600` | 컨디션 항목을 추가하고 증상·메모가 남은 자리를 채우게 함 |
| 07/30 10:17 | `322027b` | 건강 추이를 일곱 항목 한 줄로 작게 줄임 |
| 07/30 10:31 | `031d2b9` | 일정 아래 생활 습관과 이번 주 증상·메모 패널을 없앰 |
| 07/30 13:20 | `4c9784b` | 이번 주 일정 칸 간격을 줄여 한 화면에 들어오게 함 |
| 07/30 14:54 | `688e821` | 시간 미정을 Event로 바꾸고 날짜별 할 일 다섯 칸을 넣음 |
| 07/30 15:10 | `f95343f` | Event 줄을 30분 칸 높이에 맞추고 직접 입력할 수 있게 함 |
| 07/30 15:16 | `72f1141` | Event 줄의 각 칸을 타임라인 30분 칸과 정확히 같은 높이로 |
| 07/30 19:01 | `a28ae4b` | Event 줄 높이를 30분이 아니라 한 시간 칸에 맞춤 |
| 07/30 21:34 | `b5a923e` | 시간을 눌러 일정을 등록하고 색을 고를 수 있게 함 |
| 07/30 21:50 | `13133c8` | 타임라인을 새벽 5시~자정으로 넓히고 30분 눈금을 점선으로 |
| 07/30 22:21 | `dca533f` | 30분 점선 눈금을 제거 |
| 07/30 22:35 | `8101363` | 일정 색을 구글 캘린더 팔레트 24색으로 바꿈 |
| 07/31 00:04 | `bee823b` | 시간을 누르면 그 칸이 기본색으로 먼저 채워지게 함 |
| 07/31 00:11 | `003698c` | DEVLOG를 오늘까지 이어서 작성 |
| 07/31 00:14 | `c074609` | 입력 카드를 일정 칸 옆에 붙여 가리지 않게 함 |
| 07/31 00:17 | `ff96c01` | 게시글을 오늘까지의 작업으로 다시 씀 |
| 07/31 00:41 | `9c8491d` | 일정 보기를 일·주·월·년·목록으로 확장 |
| 07/31 07:50 | `ce069de` | 이동 버튼을 일정 제목 옆으로 옮기고 제목에 기간을 표시 |
| 08/01 13:22 | `f72fc4e` | 인수인계 문서(HANDOFF.md) 작성 |
| 08/01 14:52 | `e85d5aa` | 기기 간 기록 동기화 기능 추가 |
| 08/01 17:41 | `7b58885` | 홈 화면에 앱처럼 추가할 수 있게 함 |
| 08/03 00:22 | `e11ed96` | 방명록 추가 (Supabase 연동) |
| 08/07 00:45 | `b2bf6a2` | 오늘의 말씀 패널 추가 (「주님의 음성」 327일) |
| 08/07 01:01 | `747c565` | 말씀 5일 추가 (327 → 332일) |
| 08/08 00:57 | `7d5d7e7` | 말씀 32일 추가 (332 → 364일) |
| 08/08 01:15 | `a16510d` | 말씀 365일 완성 (364 → 365일) |
| 08/08 01:37 | `f1e6b38` | 말씀 패널에서 날짜를 옮길 수 있게 함 |
| 08/08 01:41 | `960a946` | 일정 이동 버튼도 < > 모양으로 통일 |
| 08/11 13:37 | `60c160e` | 동기화가 빈 칸을 '적은 것'으로 세어 저쪽 기록을 버리던 문제 수정 |
| 08/11 13:42 | `571857c` | 기능별 개발 로그와 사례글 5편 추가 |
| 08/11 15:14 | `a22e545` | 하루 기록하기에서 < > 로 날짜를 옮길 수 있게 함 |
| 08/11 17:55 | `ed5c7ad` | Todo 첫 칸 QT 고정, 상단 주 이동, 건강 추이 기간 선택 |
| 08/11 20:10 | `e5f5897` | DEVLOG에 오늘 작업(Day 5)을 이어서 추가 |
| 08/13 02:00 | `f688bce` | 푸터에서 방명록 링크를 감춤 |
| 08/13 02:14 | `d049bc1` | DEVLOG에 방명록 링크를 감춘 작업(Day 6)을 추가 |
| 08/14 20:39 | `e0ac364` | 말씀 사례글을 실제 기록과 대조해 바로잡음 |
| 08/14 20:43 | `77f4ceb` | 말씀 연동 작업의 개발 로그 추가 |
| 08/15 07:38 | `fb70e9b` | 동기화 버그 수정 작업의 사례글 추가 |
| 08/15 11:28 | `97e3f4e` | 대시보드와 상세 기록을 두 화면으로 나눔 |
| 08/17 11:50 | `ae986f0` | 사이트에 작업 기록 페이지 추가 — **32~34번 작업이 함께 들어갔다** |
| 08/17 12:31 | `1a64039` | DEVLOG에 오늘 작업(Day 7)을 적고 커밋이 어긋난 곳을 밝힘 |
| 08/17 14:28 | `538c5a0` | 빠르게 쓰기로 여섯 갈래를 적고, 남은 세 페이지를 채움 |
| 08/17 14:30 | `d61e7c4` | DEVLOG에 오늘 뒷작업(35~40)을 이어서 적음 |
| 08/17 16:02 | `13f8ec0` | 글씨를 한 가지로 통일하고 색을 복숭아빛 종이색으로 바꿈 |
| 08/17 16:57 | `4e234c9` | 받은 다섯 색 팔레트를 역할별로 넣음 |
| 08/17 16:57 | `b3c4280` | DEVLOG에 글씨·색 작업(41)을 적음 |
| 08/17 17:26 | `e70b4f1` | 일정 기본색을 이 앱의 복숭아색으로 바꿈 |
| 08/17 17:40 | `33bf0dd` | DEVLOG 41에 일정 색 이야기를 더하고 커밋 표를 정리 |
| 08/17 18:47 | `3e6e571` | 병원 방문 기록을 넣고, Event 칸과 오늘 패널을 손봄 |
| 08/17 19:05 | `d1591de` | DEVLOG에 병원 방문 작업(42)을 적음 |
| 08/17 20:12 | `3b1d220` | 공휴일을 달력에 넣고, 빠르게 쓰기를 메인화면에만 둠 |
| 08/17 21:04 | `9196cf7` | 반복 일정 — 매일·매주·매달 |

---

## 기술 스택

처음에는 의존성 없는 HTML 파일 하나였고, 지금도 화면은 그대로입니다.
기기 간 동기화와 방명록이 붙으면서 서버 쪽이 조금 생겼습니다.

- **화면**: 단일 HTML (`index.html`) — 내장 CSS + 바닐라 JavaScript + 직접 생성한 SVG 차트.
  빌드 도구 없음, 브라우저로 열면 그대로 동작
- **말씀**: `voice.js` — 읽기 전용 파일 하나. `<script src>` 로 읽으므로 빌드가 필요 없다
  (원본과 만드는 도구는 `joannefavor/voice-of-the-lord` 저장소)
- **내 기록 저장**: 브라우저 localStorage. 기본은 기기 안에만 남는다
- **기기 간 동기화**: `api/sync.js` (Vercel 서버리스) + Upstash.
  연결 코드로 짝을 맞추고, 내용은 브라우저에서 암호화해 올린다
- **방명록**: `guestbook.html` + `api/guestbook.js` + Supabase.
  Supabase 프로젝트가 사라져 **지금은 푸터 링크를 감춰 두었다** (코드는 그대로)
- **앱처럼 쓰기**: `manifest.json`, `sw.js`, 아이콘 — 홈 화면에 추가하면 앱처럼 열린다
- **배포**: Vercel (hobby 플랜) — GitHub `main` 브랜치 푸시 시 자동 배포
- **버전 관리**: Git / GitHub (비공개 저장소 `joannefavor/My-Diary`)
- **AI 도구**: Claude Code (Opus 5)

비밀 열쇠(`.env*`)와 설치 꾸러미(`node_modules/`)는 저장소에 올리지 않습니다.

---

## 주요 기능

0. **여덟 페이지와 빠르게 쓰기** *(2026-08-17 추가)*
   - 메인화면 / 일정 / 업무 / 신앙 / 건강 / 나의역사 / 미팅 / 지식창고.
     왼쪽 세로 막대로 오가고, 주소(`#/health`)에 남아 새로고침해도 그대로다
   - 여덟 페이지가 모두 제 내용을 가진다. 메인은 그날의 일정과 할 일,
     신앙은 말씀·묵상일기·기도, 미팅은 참석자·논의·할 일까지 적는다
   - **빠르게 쓰기** (메인화면): 넣을 곳을 고르고 한 줄로 적으면 제자리로 간다.
     `혈압 120/80 물 5잔 기분 4` 는 건강으로, `금요일 오후 2시 법무회의 1시간` 은 일정으로.
     고르는 차례는 왼쪽 페이지 막대와 같다
   - 달력 칸이나 `8/3`·`어제` 같은 말로 **지난 일도 적을 수 있다**
   - 해석은 브라우저 안 규칙으로만 한다. 알아들은 말만 지우고 나머지는 칸에 남긴다

1. **주간 일정 캘린더**
   - 일 / 주 / 월 / 년 / 목록 다섯 가지 보기, 제목 옆 `< >` 로 그 단위만큼 이동
   - **공휴일 표시** (2026·2027년). 대체공휴일은 이름까지 적는다
   - **반복 일정** — `매주 금요일 오후 2시 회의` 처럼 적으면 앞으로 것까지 들어간다.
     지울 때 이 하루만인지 전부인지 묻는다
   - 빈 시간을 클릭하면 그 자리가 기본색으로 채워지고 입력 카드가 옆에 뜸
   - 구글 캘린더 팔레트 24색, 색마다 글자색을 대비로 계산 (전부 WCAG AA)
   - 겹치는 일정은 가로로 나눠 배치, 현재 시각에 붉은 선

2. **Event 줄과 날짜별 Todo**
   - 시간을 정하지 않은 일정을 요일 칸에 바로 입력 (`Event`).
     칸이 낮아 체크상자는 두지 않는다 — 완료는 오늘 패널이나 목록 보기에서
   - 요일별 할 일 5칸. **첫 칸은 `QT` 로 고정**되어 체크만 한다

3. **오늘의 말씀**
   - 「주님의 음성」 365일. 어느 날을 열어도 그날 말씀이 있다
   - `< >` 로 지난 말씀도 읽을 수 있고, 일정 날짜를 옮기면 따라온다
   - 읽을거리이므로 기록 데이터와 분리 — 동기화에도 끼지 않는다

4. **주간 요약 타일 11종**
   - 전체평가를 중심으로 수면·혈압·공복혈당·운동·기분·컨디션·수분·식사·복약·체중
   - 판정이 색·기호·글자로 함께 표시 (색만으로 구분하지 않음)

5. **건강 추이 차트 7종 + 기간 선택**
   - **주 / 월 / 연도 / 기간 지정** 중에 고른다. 연도는 한 달이 한 점(그 달 평균)
   - 카드마다 최근값을 크게, 마우스를 올리면 그 시점 값과 판정
   - 같은 데이터를 `표로 보기`로도 제공

6. **병원 방문 기록** *(2026-08-17 추가)*
   - 병원 이름 · 방문일 · 내용. 평소엔 한 줄씩만 보이고 누른 것만 펼쳐진다
   - 병원 고르기 · 찾기 · 날짜별/병원별 보기로 조회한다. 방문이 늘어도 화면이 길어지지 않는다

7. **하루 기록하기**
   - `< >` 로 하루씩 이동. 주를 벗어나면 요약·차트도 함께 따라간다
   - 수면은 취침·기상을 오전/오후 · 시 · 분(5분 단위)으로 골라 시간 자동 계산
   - 끼니마다 먹은 것과 먹은 약을 나란히 기록

8. **기기 간 기록 동기화**
   - 연결 코드로 휴대폰과 PC가 같은 기록을 본다
   - 빈 칸은 '적은 것'으로 세지 않아 한쪽 기록이 덮이지 않는다

9. **방명록** — 현재 감춤
   - 다른 사람이 글을 남길 수 있다 (Supabase 연동)
   - Supabase 프로젝트가 사라져 푸터 링크를 감춰 둔 상태.
     새 프로젝트를 만들고 열쇠만 바꾸면 되살아난다

10. **기록하지 않은 값과 0의 구분**
   - 적지 않은 항목은 0으로 판정하지 않고 `기록 없음`으로 남김

11. **데이터 보관과 접근성**
   - 기록은 브라우저에 저장, JSON 내보내기/가져오기로 이동·백업
   - 홈 화면에 앱처럼 추가 가능
   - 밝은/어두운 테마 각각 설계, 좁은 화면에서 단일 열로 접힘
