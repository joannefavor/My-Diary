# 주간 기록부 - 개발 로그 (「주님의 음성」 365일 말씀 연동)

AI 코딩 도구와 함께 진행한 개발 작업 기록입니다.

카카오톡으로 3년간 받은 묵상 글과 책 사진에서 365일치 말씀을 뽑아,
주간 기록부 대시보드에 "오늘의 말씀" 패널로 붙인 작업입니다.

- **기간**: 2026-08-06 밤 ~ 2026-08-08 밤 (사흘, 주로 심야)
- **도구**: Claude Code (Claude Opus 5)
- **저장소 두 곳**: 원본·도구는 `voice-of-the-lord`(신규), 앱은 `My-Diary`

---

## 2026-08-06 (Day 1)

### 1. 작업 폴더 만들고 옮기기

```
폴더변경하려고
```
```
/Users/yeon/gpters/주님의음성
```

**Claude Code 작업:**
- 대상 폴더가 비어 있는 것을 확인 (OMC 상태 파일만 존재)
- 한글 폴더명이 앞선 목록에 안 보인 이유가 자모 정규화(NFD/NFC) 차이임을 안내

---

### 2. 365일 말씀 정리 — 요구사항 확인

```
주님의 음성이라는 규장에서 나온 책인데 매일의 말씀이 365일 있거든
이거 날짜별로 찾아서 정리 가능할까?
```

**Claude Code 작업:**
- **저작권이 있는 출판물의 본문을 기억에서 생성할 수 없음**을 먼저 밝힘. 원본이 있어야 정리가 가능하다고 안내
- 기획 질문(AskUserQuestion)으로 두 가지를 확인

**기획 Q&A:**

```
Q. 원본 자료를 어떤 형태로 갖고 계신가요?
   선택지: PDF/EPUB · 텍스트/HWP/워드 · 종이책 사진/스캔 · 아직 원본 없음(구조만 먼저)
A. 아직 원본 없음 — 구조만 먼저

Q. 정리 결과를 어떤 형태로 받고 싶으신가요?
   선택지: 날짜별 마크다운 365개 · JSON/CSV 한 파일 · 검색되는 웹페이지 · 기존 dairy_report 앱에 연동
A. 기존 dairy_report 앱에 연동
```

- 기존 앱 구조 조사 — `index.html` 한 파일(3,365줄), 의존성·빌드 도구 없음, localStorage 저장
- `HANDOFF.md`에서 지켜야 할 제약 확인 (입력 중 재렌더 금지, 테마 변수 3중 스코프 등)

---

### 3. 365일 빈 틀과 빌드 도구 만들기

**Claude Code 작업:**
- `주님의음성/build.mjs` — 의존성 없는 빌드 스크립트 (Node 18+)
  - `--init` 365일 빈 틀 생성, `--check` 검증, `--to <폴더>` 앱에 복사
  - 2월은 28일까지 (책이 365일치라 2/29 없음)
- `주님의음성/content/01.md ~ 12.md` — 열두 달 파일에 365일 틀
- 산출물을 `voice.js`(전역 객체)로 만드는 방식 채택
  - `fetch`가 아니라 `<script src>`로 읽어 `file://`에서도 동작
  - 앱의 "빌드 도구·의존성 없음" 제약을 깨지 않음

---

### 4. 앱에 "오늘의 말씀" 패널 연동

**Claude Code 작업:**
- `index.html` — 요약 타일 아래에 `#voicePanel` 추가
- `renderVoice()`를 `renderExceptForm()`에서 호출 → 일정에서 날짜를 옮기면 말씀도 따라감
- **말씀은 기록이 아니라 읽을거리**이므로 `state`에 넣지 않음. localStorage 저장·기기 간 동기화 모두 제외
- 세 경우를 모두 견디게 처리
  - `voice.js` 없음 → 패널째 숨김
  - 있지만 그날은 빈 날 → "아직 옮겨 적지 않았습니다"
  - 채워둠 → 제목·구절·본문 출력
- 헤드리스 크롬으로 세 경우 모두 렌더링 검증. `<b>` 태그가 글자 그대로 나오는 것으로 이스케이프도 확인

---

### 5. 휴대폰에서 보기 — 배포 방식 결정

```
휴대폰에서도 볼거야
```

**Claude Code 작업:**
- 앱에 이미 있는 동기화 암호(PBKDF2 + AES-GCM)를 재사용할 수 있음을 확인하고 선택지로 제시

**기획 Q&A:**

```
Q. 휴대폰에서 말씀을 어떻게 보시겠어요?
   선택지: 암호로 잠가서 배포(추천) · 집 안 로컬 서버 · Vercel 유료 플랜 · 그냥 공개로 올리기
A. 그냥 공개로 올리기
```

- 공개 주소에 책 본문이 올라간다는 점을 알린 뒤, 결정에 따라 진행
- `sw.js` 수정 — `/voice.js`를 화면과 같은 **"인터넷 먼저"** 규칙으로 분리
  - 기본 규칙(사본 먼저)에 두면 홈 화면 앱이 처음 받은 사본만 계속 써서 새로 채운 날이 영영 안 보임
  - 받아올 때마다 사본은 갱신 → 오프라인에서도 마지막 말씀은 읽힘
- 서비스 워커 분기를 Node에서 직접 태워 6가지 경우 검증 (인터넷 먼저 / 사본 갱신 / 오프라인 폴백 / 빈 응답 / 아이콘은 사본 먼저 / `/api/`는 가로채지 않음)

---

## 2026-08-07 (Day 2)

### 6. 카카오톡 대화에서 말씀 뽑아내기

```
/Users/yeon/Downloads/KakaoTalk_Chat_이경민B_2026-08-07-00-29-04.csv
```

**Claude Code 작업:**
- 대화 분석 — 1,381건 중 **543건이 말씀**, 2018-01-11 ~ 2020-07-10
- 꼬리말에서 출처 확인 — 「주님의 음성」, 두 명의 경청자
- 일상 대화 838건은 읽지도 쓰지도 않음
- `주님의음성/import-kakao.mjs` 작성 — CSV 파서부터 직접 구현 (메시지 안에 줄바꿈이 있어 줄 단위로 자르면 안 됨)

**형식 흔들림 흡수 (실제로 겪은 것):**

| 흔들림 | 예 |
|---|---|
| 달 이름 오타 | `HARCH`(MARCH), `MRY`(MAY), `JUNI`(JUNE) → 편집거리로 매칭 |
| 숫자 자리에 알파벳 | `1O.12` — 숫자 0 대신 대문자 O |
| 달 이름 없는 머리글 | `🇰🇷 5. 29(수)` |
| 첫 줄이 빈 줄 | 머리글이 둘째 줄부터 |
| 구절 앞 기호 4가지 | 🌱 🌴 ☘ 💒 |
| 꼬리말 8가지 | `***주님의 음성***`, `☆출처: …` 등 |

**세 번 고친 버그:**
1. 달 이름 오타 → 접두사 매칭 실패. 편집거리(Levenshtein)로 교체
2. **빈 날 판정 버그** — `\s`가 줄바꿈까지 먹어 `title:` 다음 줄 글자를 값으로 오인. 327일 전부 "이미 채워짐"으로 건너뜀. `[^\S\n]`으로 수정
3. **꼬리말 위치가 해마다 다름** — 2018년형은 구절 뒤, 2019년형은 구절 앞. "꼬리말에서 끊기"로 짰더니 2019년 이후 구절이 통째로 소실, 구절 없는 날이 2일 → 78일로 급증. 꼬리말을 끊는 지점이 아니라 **먼저 걷어낼 줄**로 바꿔 해결

- 결과: **327일 / 365일**. 543건 전부 날짜 인식, 중복 179일은 가장 온전한 판본 선택
- 감사 결과 — 꼬리말 잔재 0건, 이모지 잔재 0건, 본문 평균 391자

---

### 7. 커밋·푸시와 배포 확인

```
커밋하고 푸시해줘
```

**Claude Code 작업:**
- `My-Diary`에 커밋 (`index.html`, `sw.js`, `voice.js`) 후 푸시
- 배포 반영을 실제로 확인 — `voice.js` HTTP 200, 420,429 bytes, 327일
- 이전 세션의 미추적 문서 4개는 무관하여 제외

---

### 8. 말씀 저장소 분리

```
주님의음성 폴더도 git 저장소로 만들어줘
```
```
진행해줘
```

**Claude Code 작업:**
- `git init` — 원본(`content/*.md`)과 도구(`build.mjs`, `import-kakao.mjs`)만 추적
- `voice.js` / `voice.json`은 산출물이라 `.gitignore`
- **복제본에서 빌드해 검증** — 만들어진 `voice.js`가 배포본과 바이트 단위로 동일
- GitHub **비공개** 저장소 `voice-of-the-lord` 생성 후 푸시
  - `isPrivate: true` API로 직접 확인, `voice.js` 미포함(404) 확인

---

### 9. 책 사진으로 빈 날 채우기 (1차)

```
하나씩 다시 보낼게
```

**Claude Code 작업:**
- 처음 받은 합본 캡처는 **726×552px 안에 페이지 8장** → 한 페이지 160px, 본문 한 줄 2~3px
- **판독 불가임을 알리고 추측 입력을 거부** — "말씀을 지어내는 셈"
- 개별 사진으로 다시 받아 5일 추가 (01-05, 01-12, 02-04, 03-08, 03-13)
- 두 쪽에 걸쳐 잘린 02-10, 03-11은 `PENDING.md`에 보관 (잘린 채로 넣으면 앱에서 문장이 끊겨 보임)
- 저해상도 사진에서 판독한 `내`/`네` 두 곳을 고해상도로 검증 — 판단이 맞았음을 확인

```
지금까지 넣은 거 커밋하고 푸시해줘
```

- 양쪽 저장소에 각각 커밋·푸시, 배포 반영까지 확인 (332일)

---

### 10. 책 사진 40장 (2차)

```
다른 사진도 보낼게
```

**Claude Code 작업:**
- 40장 판독 → **32일치 확보**. 보류 중이던 02-10, 03-11의 뒷장도 포함
- 두 쪽에 걸친 6일은 이음새를 문자열로 대조해 확인
- `build.mjs`에 `title_en`(영문 제목) 항목 추가

---

## 2026-08-08 (Day 3)

### 11. 진행 상황 확인

```
뭐 문제있어?
```

**Claude Code 작업:**
- 문제 없음. 32일치 본문을 한 번에 넣는 스크립트를 작성 중이었음을 알리고 계속 진행
- 결과 **364일 / 365일**, 감사 통과 (문장 잘림 0건, 본문 100자 미만 0건)

---

### 12. 12월 17일 확인 요청

```
12월17일은 그렇게 끝나는게 맞네
```

**Claude Code 작업:**
- 455쪽이 문장으로 온전히 끝나 완결로 판단했으나, 확신이 없어 사용자에게 확인 요청했던 건
- 확인 결과 그대로 유지

---

### 13. 마지막 하루 — 365일 완성

**Claude Code 작업:**
- 354쪽을 받아 `09-27 내가 기다리는 까닭 / Divine Restraint` 완성
- 353쪽의 `…너를 도우려는 갈망` + 354쪽의 `과 조급함을 억제하는…` 이음새 검증
- **365일 / 365일** — 빠진 날 0, 달력에 없는 날 0, 꼬리말 잔재 0
- 영문 제목 364일(04-04만 책에 없음), 성경 구절 318일
- 기다릴 것이 없어져 `PENDING.md` 삭제

---

### 14. 휴대폰 확인과 기기 간 동기화 문제

```
휴대폰에서 확인해볼게
```
```
핸드폰에서 잘 보이네
```
```
근데 핸드폰에서 내가 입력한 값이 PC 로 연동이 안 되는데 왜 그러지?
```

**Claude Code 작업:**
- 배포된 `/api/sync`에 직접 요청해 서버 정상 확인 (HTTP 200, 잘못된 id는 400)
- 원인 진단 — 동기화는 **비밀 코드를 넣어 켜는 opt-in 기능**. 세 가지 가능성 제시
  1. 두 기기 모두에서 켜지 않음 (가장 흔함)
  2. PC에서 `file://`로 열면 동기화가 아예 차단됨 + 저장 공간도 별개
  3. 양쪽 코드 불일치

```
PC에서 배포 주소로 열어서 코드 넣어볼게
```
```
연결됐어, 이제 잘 되네
```

---

### 15. 비밀 코드 보관 방법

```
코드 어디에 적어두면 좋을까
```

**Claude Code 작업:**
- macOS `암호` 앱(키체인) + 종이 사본 권장. 코드는 재발급 불가
- **저장소 안에 적으면 안 되는 이유** 설명 (커밋·배포에 딸려 감)
- 두 저장소의 현재 파일과 **전체 커밋 이력**을 검사해 코드 유출이 없음을 확인. `.env.local`이 `.gitignore`에 걸려 있는 것도 확인

---

### 16. 말씀 패널에 날짜 이동 추가

```
오늘의 말씀 페이지에서 날짜 이동 할 수 있게 만들고 싶어
```

**기획 Q&A:**

```
Q. 말씀 패널의 날짜 화살표를 누를 때 어떻게 동작할까요?
   선택지: 말씀만 움직임(추천) · 앱 전체 날짜가 같이 움직임 · 말씀만 움직이고 달력도 붙임
A. 말씀만 움직임
```

작업 중 추가 요청:

```
< > 모양으로 움직이게
```
```
날짜로도 움직이고
```

**Claude Code 작업:**
- 머리글에 `<` `>` 버튼, 날짜 입력칸, `오늘` 버튼 추가
- 말씀이 자체 날짜(`voicePick`)를 가짐
  - `< >`와 날짜 칸은 말씀만 이동, 일정·기록 화면은 그대로
  - 일정에서 날짜를 옮기면(`selected` 변경) 다시 그날로 따라붙음
  - 오늘이 아니면 `오늘` 버튼 노출, 날짜 칸을 비우면 다시 따라감
- **`render()`가 아니라 `renderVoice()`만 호출** — `render()`는 입력 폼을 다시 그려서 적던 값이 날아감 (`HANDOFF.md`가 경고한 함정)
- 머리글도 전환 — 오늘이면 `오늘의 말씀`, 아니면 `8월 9일 말씀`
- 브라우저에서 실제 클릭 검증 — `>`·`<` 이동, 일정 제목 불변, 날짜 직접 입력, `오늘` 복귀, 일정 이동 시 따라오기
- 좁은 화면(패널 320px)에서 머리글이 두 줄로 접히고 가로 넘침 없음을 측정으로 확인

---

### 17. 일정 이동 버튼 모양 통일

```
일정 달력의 날짜 이동도 화살표 말고 < > 로 옮길 수 있게 모양 바꿔줄래?
```

**Claude Code 작업:**
- `#prevWeek` / `#nextWeek`의 `←` `→` → `<` `>` (접근성 라벨은 유지)
- 주 보기·일 보기 모두 이동 정상 동작 확인, 배포본에 남은 화살표 0개 확인

---

## 커밋 히스토리

### voice-of-the-lord (원본·도구, 비공개)

| 날짜 | 커밋 | 설명 |
|------|------|------|
| 08/07 | `2272404` | 「주님의 음성」 365일 말씀 정리 저장소 |
| 08/07 | `b80dee3` | 책에서 5일 옮겨 적음 (329 → 332일) |
| 08/08 | `97507cc` | 책에서 32일 옮겨 적음 (332 → 364일) |
| 08/08 | `3d874f2` | 365일 완성 (364 → 365일) |

### My-Diary (앱)

| 날짜 | 커밋 | 설명 |
|------|------|------|
| 08/07 | `b2bf6a2` | 오늘의 말씀 패널 추가 (「주님의 음성」 327일) |
| 08/07 | `747c565` | 말씀 5일 추가 (327 → 332일) |
| 08/08 | `7d5d7e7` | 말씀 32일 추가 (332 → 364일) |
| 08/08 | `a16510d` | 말씀 365일 완성 (364 → 365일) |
| 08/08 | `f1e6b38` | 말씀 패널에서 날짜를 옮길 수 있게 함 |
| 08/08 | `960a946` | 일정 이동 버튼도 < > 모양으로 통일 |
| 08/14 | `e0ac364` | 말씀 사례글을 실제 기록과 대조해 바로잡음 |

---

## 기술 스택

- **Frontend**: 단일 `index.html` (HTML + CSS + 순수 JS). 프레임워크·빌드 도구 없음
- **말씀 데이터**: `voice.js` — `window.VOICE`에 담긴 전역 객체 (449KB, 365일)
- **정리 도구**: Node 18+ 스크립트 2개 (`build.mjs`, `import-kakao.mjs`). 의존성 없음
- **오프라인**: 서비스 워커 (`sw.js`) — 화면과 `/voice.js`는 인터넷 먼저, 아이콘은 사본 먼저
- **Deployment**: Vercel (`main` 푸시 시 자동 배포)
- **검증**: 헤드리스 크롬(렌더링·클릭), `node --check`(문법), Node 단위 검증(서비스 워커 분기)

---

## 주요 기능

1. **오늘의 말씀 패널**
   - 요약 타일 바로 아래. 한글 제목 · 영문 제목 · 인용 구절(장절) · 본문
   - 일정에서 날짜를 옮기면 말씀도 따라감

2. **말씀 날짜 이동**
   - `<` `>` 하루씩, 날짜 칸으로 임의 날짜 점프, `오늘` 버튼으로 복귀
   - 말씀만 움직이고 일정·기록 화면은 건드리지 않음

3. **365일 말씀 데이터**
   - 카톡(2018~2020) 327일 + 책 사진 38일
   - 윤년 2월 29일에는 2월 28일 말씀을 보여주고 그 사실을 표시

4. **카카오톡 가져오기 도구**
   - 대화 CSV에서 말씀만 추출. 일상 대화는 읽지 않음
   - 기본은 미리보기, 이미 채운 날은 건드리지 않음(`--force`로 덮어쓰기)
   - 달 이름 오타·기호·꼬리말 위치 등 형식 흔들림 흡수

5. **원본과 산출물 분리**
   - 원본은 별도 비공개 저장소, `voice.js`는 언제든 재생성 가능
   - 말씀은 기록과 완전히 분리 — 저장소·동기화에 끼지 않음
