# HANDOFF — 주간 기록부 (My Diary)

이 프로젝트를 이어받는 사람(또는 다음 세션)을 위한 인수인계 문서입니다.
2026-08-01 기준.

---

## 1. 한눈에

| | |
|---|---|
| **무엇** | 일주일 일정과 건강 상태를 한 화면에서 기록하고 추이로 보는 개인 대시보드 |
| **사이트** | https://my-diary-six-sigma.vercel.app |
| **저장소** | https://github.com/joannefavor/My-Diary (**비공개**) |
| **배포** | Vercel `my-diary` (joan-s-projects1 팀, hobby 플랜) |
| **자동 배포** | `main` 푸시 → 15초~2분 내 반영 |
| **규모** | `index.html` 한 파일 2,892줄 (JS 1,947 / CSS 876 / 함수 109개) |
| **커밋** | 36개 |
| **의존성** | **없음.** 빌드 도구·프레임워크·패키지 매니저 전부 없음 |

의존성이 없다는 게 이 프로젝트의 핵심 제약입니다. `index.html`을 브라우저로 열면 그대로 동작하고,
Vercel은 이 파일을 정적으로 서빙만 합니다. 빌드 단계가 없으므로 `npm install` 같은 것도 없습니다.

---

## 2. 파일 구성

```
health_care/
├── index.html          모든 것 (HTML + CSS + JS + SVG 차트)
├── README.md           기록 항목, 판정 기준 출처, 데이터 설명
├── DEVLOG.md           개발 로그 (26개 작업, 7/25~7/31)
├── AI_CASE_STUDY.md    비개발자 대상 사례 게시글 초안 (검토 대기)
├── HANDOFF.md          이 문서
├── .gitignore          .DS_Store / .vercel / .omc / screenshots/
├── .vercelignore       배포에 불필요한 문서·이미지 제외
├── .vercel/            Vercel 프로젝트 링크 (gitignore됨, 옮길 때 함께 가져가야 함)
└── screenshots/        작업 중 만든 검증 이미지 25개 (gitignore됨)
```

---

## 3. 데이터 모델

브라우저 `localStorage`에만 저장합니다. 서버 없음, 계정 없음, 전송 없음.

- **저장 키**: `weekly-health-ledger-v3`
- **보기 상태 키**: `weekly-health-ledger-view`

```
state = {
  days: { "2026-07-31": <하루 기록>, ... },
  meds: [ { id, name }, ... ]        // 등록해 둔 약 목록
}
```

하루 기록 한 건의 모양 (`blank()` 참고):

```
{
  schedule: [ { id, time, end, title, done, color } ],   // time 이 "––:––" 이면 시간 미정(Event)
  todos:    [ { text, done } ],                          // 최대 5칸
  sleep:    { from: "23:30", to: "07:00" },               // 예전 기록은 숫자(시간)일 수 있음
  bp:       { s: 118, d: 76 },
  glucose:  92,  weight: 64.5,  water: 8,
  exercise: { min: 40, kind: "달리기" },
  meals:    { b: true, l: true, d: false },               // 끼니 체크
  menu:     { b: "현미밥", l: "", d: "" },                 // 끼니별 식단
  pill:     { b: "혈압약", l: "", d: "" },                 // 끼니별 복약
  meds: false, medTaken: {},                              // 예전 복약 방식 (호환용)
  mood: 4, condition: 4, overall: 4, note: ""
}
```

### 하위 호환에 관한 규칙

기록은 사용자의 실제 데이터이므로 **형식이 바뀌어도 예전 기록을 계속 읽어야 합니다.**
지금까지 그렇게 처리한 곳:

- `sleep` — 예전에는 숫자(7.5), 지금은 `{from, to}`. `sleepHours()`가 둘 다 처리
- `color` — 6색 시절 값(1~6)을 24색 팔레트로 옮김 (`EV_LEGACY`, `normalize()`)
- `meds` / `medTaken` — 약 등록 방식으로 적어둔 날도 복약한 날로 인정 (`medDone()`)
- `menu` / `pill` / `todos` — 나중에 생긴 항목이라 없을 수 있음. `day()`에서 채워 넣음

형식을 또 바꾼다면 같은 방식으로 처리하세요. 저장 키를 올리는 것(`v3` → `v4`)은
**기존 기록을 버리는 것**이므로 마지막 수단입니다.

---

## 4. 화면 구조

위에서부터:

1. **머리글** — 건강 요약이 어느 주 것인지 + `오늘` 버튼
2. **주간 요약 타일 11개** — 전체평가(큰 숫자) / 수면 / 혈압 / 공복혈당 / 운동 / 기분 / 컨디션 / 수분 / 식사 / 복약 / 체중
3. **일정 패널** — `← 기간 →` + 보기 토글(일·주·월·년·목록)
4. **건강 추이** — 작은 차트 7개를 한 줄로, 아래에 `표로 보기`
5. **하루 기록하기** — 날짜 탭 + 입력 폼
6. **푸터** — 전체 지우기 / JSON 내보내기 / 가져오기

### 일정 보기 다섯 가지

| 보기 | 함수 | 설명 |
|---|---|---|
| 일 | `calMarkup()` | 열 1개 (`--cols` CSS 변수로 조절) |
| 주 | `calMarkup()` | 열 7개, 기본값 |
| 월 | `monthMarkup()` | 6주 × 7일 격자, 칸당 일정 3건 + `+N건` |
| 년 | `yearMarkup()` | 작은 달력 12개, 일정 있는 날에 점 |
| 목록 | `listMarkup()` | 요일별 목록 |

`← →` 이동은 `step(dir)` 하나가 보기별 단위(하루/한 주/한 달/한 해)로 처리합니다.

**건강 요약·차트·하루 기록은 항상 `weekStart` 주를 따릅니다.** 월·년 보기로 바꿔도
이 부분은 주 단위입니다. 건강 지표가 주간 평균으로 설계돼 있기 때문입니다.

---

## 5. 반드시 알아야 할 함정

이 프로젝트에서 실제로 터졌던 버그들입니다. 같은 실수를 반복하기 쉬우니 꼭 읽으세요.

### (1) 입력 중 화면을 다시 그리면 입력이 날아갑니다 ★가장 중요

`persist()`는 `save()` + `render()`이고, `render()`는 **입력 폼 전체를 다시 그립니다.**
값이 여러 조각으로 나뉘어 들어오는 입력(드롭다운 3개, 수축기/이완기 2칸)에서는
한 조각만 고른 상태에서 저장할 값이 없어 → 폼이 빈 상태로 리셋 → **방금 고른 게 지워집니다.**

이 때문에 **취침 시각과 혈압이 아예 저장되지 않던 시기가 있었습니다.**

해결책은 `renderExceptForm()` — 입력 폼을 뺀 나머지만 갱신합니다.
수면·혈압·할 일 입력이 이 방식을 씁니다. 새로 입력 항목을 추가할 때
**"한 번에 완성되지 않는 입력"이면 반드시 이 경로를 쓰세요.**

### (2) 테마 변수는 세 곳에 넣어야 합니다

색 변수는 아래 **세 스코프 모두**에 정의해야 합니다. 하나라도 빠지면 조용히 깨집니다.

```
:root { ... }                                  밝은 테마
@media (prefers-color-scheme: dark)
  :root:not([data-theme="light"]) { ... }      시스템 설정이 다크일 때
:root[data-theme="dark"] { ... }               앱 안에서 테마를 직접 바꿨을 때
```

실제로 `--s3`(컨디션 차트 색)이 세 번째에 빠져서, 시스템 다크에서는 멀쩡한데
**테마 토글로 다크를 켜면 색이 깨지는** 상태였습니다.
치환으로 CSS를 편집할 때 들여쓰기가 같아 엉뚱한 블록에 들어가기 쉬우니
**넣은 뒤 세 스코프를 각각 확인**하세요.

### (3) 헤드리스 브라우저는 로드 후 resize를 발생시킵니다

스크린샷을 찍으면 `resize` 이벤트가 발생합니다. 예전에 `resize → 입력 카드 닫기`로
해놨더니 **캡처에 카드가 안 잡혔고**, 파고들어 보니 실제로도
**휴대폰에서 키보드가 올라오면 적던 내용이 날아가는** 버그였습니다.
지금은 닫지 않고 위치만 다시 잡습니다(`place()`).

캡처에 뭔가 안 나오면 코드가 아니라 캡처 환경을 의심해 보세요.
DOM 측정(`--dump-dom`)으로 먼저 확인하는 게 빠릅니다.

### (4) 죽은 코드는 그 자리에서 지우세요

기능을 없앨 때 마크업만 지우면 렌더 함수·이벤트 핸들러·CSS가 남아
`getElementById(...)` 가 `null`이 되어 터집니다. 실제로 패널 두 개를 없앴을 때
관련 코드 194줄을 함께 지웠습니다. 지운 뒤에는 **중복·미사용 함수를 훑어 확인**하세요.

### (5) 기록하지 않은 것과 0은 다릅니다

요약은 **그 항목을 실제로 적은 날만** 분모로 삼습니다.
운동을 안 적은 주에 "0분 부족"이라고 판정하면 안 됩니다.
새 지표를 추가할 때도 이 규칙을 지키세요. 복약이 비율(%) 대신
**복용한 날 수**인 이유도 같습니다 — 체크 없는 날이 "안 먹음"인지 "안 적음"인지
구분할 수 없어서 비율 자체가 거짓말이 되기 때문입니다.

---

## 6. 지켜온 규칙

- **판정 기준에는 출처를 밝힙니다.** 혈압은 대한고혈압학회 진료지침, 운동은 WHO 주 150분,
  혈당은 공복 기준. 그리고 화면과 README 양쪽에 "진단이 아니라 참고"임을 적습니다.
- **상태를 색만으로 표시하지 않습니다.** 색 + 기호(●▲■) + 글자를 함께 씁니다.
- **일정 색 24가지는 각각 글자색을 대비로 계산**해 지정했습니다. 전부 WCAG AA(4.5:1) 이상,
  최저 4.92:1. 색을 추가하면 대비를 다시 계산하세요.
- **없는 데이터를 지어내지 않습니다.** 사용자의 실제 일정을 모르면 비워두고 묻습니다.
- **고쳤다고 말하기 전에 확인합니다.** 헤드리스 브라우저로 실제 렌더링·클릭·저장을 검증하고,
  계산 로직은 경우별로 테스트합니다(수면 시각 8가지, 12/24시간 변환 8가지 등).

---

## 7. 개발·배포 방법

```bash
# 열기 — 서버 필요 없음
open index.html

# 배포 (main 푸시하면 자동)
git commit -am "..." && git push

# 자동 배포가 안 걸릴 때 직접
vercel deploy --prod --yes --scope joan-s-projects1
```

### 검증에 쓴 방법 (헤드리스 크롬)

```bash
# 화면 캡처
"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" \
  --headless --disable-gpu --no-sandbox --hide-scrollbars \
  --virtual-time-budget=6000 --window-size=1280,2400 \
  --screenshot=/tmp/shot.png "file://$PWD/index.html"

# 동작 검증 — 테스트 스크립트를 덧붙인 사본을 만들어 document.title 로 결과를 받는다
#   (localStorage 를 미리 주입해 데이터가 있는 상태를 만들 수 있다)
--dump-dom "file:///tmp/_test.html"
```

문법 검사는 `<script>` 블록만 떼어내 `node --check`로 했습니다.

---

## 8. 지금 상태와 남은 일

### 동작하는 것
일정(일·주·월·년·목록 보기, 클릭 등록, 24색, Event, Todo), 건강 11개 항목 기록,
주간 요약 타일, 차트 7종, 표 보기, JSON 내보내기/가져오기, 밝은·어두운 테마, 좁은 화면 대응.

### 남은 일

| 항목 | 상태 |
|---|---|
| `AI_CASE_STUDY.md` 검토 | 초안 완성, 사용자 확인 대기 |
| 실제 일정 채우기 | 사용자가 본인 일정을 알려주지 않아 비워둠 |
| 기기 간 기록 동기화 | 지금은 JSON 내보내기/가져오기 수동. 사용자가 원하는 다음 기능 |
| 휴대폰 사용성 | 화면은 접히지만, 홈 화면 바로가기·입력 단순화 여지 있음 |

### 알아둘 제약

- **Vercel hobby 플랜이라 프로덕션 주소에 암호를 걸 수 없습니다.** 공개 주소입니다.
  다만 기록은 브라우저에만 있어 남에게 보이지 않습니다.
- **저장소는 비공개**입니다. 공개로 바꾸면 코드가 공개되며, 기록은 어차피 저장소에 없습니다.
- GitHub 자동 배포가 한 번 조용히 멈춘 적이 있습니다. 푸시 후 2분이 지나도 반영되지 않으면
  `vercel git disconnect` → `vercel git connect`로 다시 맺으면 됩니다.

---

## 9. 폴더를 옮길 때

`.vercel/`은 gitignore 대상이라 `git clone`으로는 따라오지 않습니다.
**폴더째 옮기거나, 옮긴 뒤 `vercel link`로 다시 연결하세요.**

```bash
# 폴더째 옮기기 (git 이력·리모트·Vercel 링크 모두 유지)
cp -R /Users/yeon/gpters/health_care/. /Users/yeon/gpters/dairy_report/

# 옮긴 뒤 확인
cd /Users/yeon/gpters/dairy_report
git log --oneline -1        # 최신 커밋이 보이면 이력 정상
git remote -v               # origin 이 My-Diary 를 가리키면 정상
ls .vercel/project.json     # 있으면 vercel 명령이 바로 동작
```

`.omc/`는 도구가 만드는 로컬 상태 폴더라 옮기지 않아도 됩니다.
